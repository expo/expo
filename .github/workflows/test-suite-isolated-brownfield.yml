name: Test Suite Isolated Brownfield

on:
  workflow_dispatch: {}
  pull_request:
    paths:
      - .github/workflows/test-suite-isolated-brownfield.yml
      - apps/minimal-tester/**
      - packages/expo/**
      - packages/expo-brownfield/**
      - packages/expo-modules-core/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios-build:
    strategy:
      fail-fast: true
      matrix:
        build-type: [debug, release]
    runs-on: macos-15
    steps:
      - name: 👀 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
      - name: 🔨 Switch to Xcode 26.0
        run: sudo xcode-select --switch /Applications/Xcode_26.0.app
      - name: ➕ Add `bin` to GITHUB_PATH
        run: echo "$(pwd)/bin" >> $GITHUB_PATH
      - name: 💎 Setup Ruby and install gems
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: 3.2.2
      - name: ♻️ Restore caches
        uses: ./.github/actions/expo-caches
        id: expo-caches
        with:
          yarn-workspace: "true"
          yarn-tools: "true"
      - name: 🧶 Install node modules in root dir
        if: steps.expo-caches.outputs.yarn-workspace-hit != 'true'
        run: yarn install --frozen-lockfile
      - name: 🍏 Build iOS artifacts
        working-directory: ./apps/minimal-tester
        run: |
          npx expo prebuild --clean --platform ios
          npx expo-brownfield build-ios --${{ matrix.build-type }} --verbose
        shell: bash
        env:
          NODE_ENV: production
      - name: 🔔 Notify on Slack
        uses: ./.github/actions/slack-notify
        if: failure() && (github.event.ref == 'refs/heads/main' || startsWith(github.event.ref, 'refs/heads/sdk-'))
        with:
          webhook: ${{ secrets.slack_webhook_ios }}
          channel: "#expo-ios"
          author_name: Isolated Brownfield Test Suite (iOS)
  android-build:
    runs-on: ubuntu-24.04
    env:
      ORG_GRADLE_PROJECT_reactNativeArchitectures: x86_64
      GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx4096m -XX:MaxMetaspaceSize=4096m"
    steps:
      - name: 👀 Checkout
        uses: actions/checkout@v5
      - name: 🧹 Cleanup GitHub Linux runner disk space
        uses: ./.github/actions/cleanup-linux-disk-space
      # TODO(pmleczek): Remove (?)
      # - name: 🚀 Setup Bun
      #   uses: oven-sh/setup-bun@v2
      #   with:
      #     bun-version: latest
      - name: 🔨 Use JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"
      - name: ♻️ Restore caches
        uses: ./.github/actions/expo-caches
        id: expo-caches
        with:
          gradle: "true"
          yarn-workspace: "true"
          yarn-tools: "true"
          react-native-gradle-downloads: "true"
      - name: 🧶 Install node modules in root dir
        if: steps.expo-caches.outputs.yarn-workspace-hit != 'true'
        run: yarn install --frozen-lockfile
      - name: 🤖 Build and publish Android artifacts
        run: |
          npx expo prebuild --clean --platform android
          npx expo-brownfield build-android --repo MavenLocal --all --verbose
        shell: bash
        working-directory: ./apps/minimal-tester
        env:
          NODE_ENV: production
      - name: 🔔 Notify on Slack
        uses: ./.github/actions/slack-notify
        if: failure() && (github.event.ref == 'refs/heads/main' || startsWith(github.event.ref, 'refs/heads/sdk-'))
        with:
          webhook: ${{ secrets.slack_webhook_android }}
          channel: "#expo-android"
          author_name: Isolated Brownfield Test Suite (Android)
