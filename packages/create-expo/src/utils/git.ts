import spawnAsync from '@expo/spawn-async';
import chalk from 'chalk';
import prompts from 'prompts';

import { env } from './env';
import { profileAsync } from './profiler';

const debug = require('debug')('expo:init:git') as typeof console.log;

type GitStatus = 'not-installed' | 'not-in-repo' | 'in-repo';

/**
 * Single spawn to check both git availability and repo status.
 * - ENOENT → git not installed
 * - non-zero exit → git installed, not inside a repo
 * - zero exit → git installed, inside a repo
 */
async function checkGitStatusAsync(root: string): Promise<GitStatus> {
  try {
    await spawnAsync('git', ['rev-parse', '--is-inside-work-tree'], { stdio: 'ignore', cwd: root });
    return 'in-repo';
  } catch (error: any) {
    // When the binary isn't found, Node emits ENOENT
    if (error?.code === 'ENOENT' || error?.errno === 'ENOENT') {
      return 'not-installed';
    }
    // Non-zero exit means git is installed but we're not in a repo
    return 'not-in-repo';
  }
}

export async function initGitRepoAsync(root: string) {
  const gitStatus = await profileAsync('git:check', () => checkGitStatusAsync(root));

  if (gitStatus === 'not-installed') {
    debug(chalk.dim('Unable to initialize Git repo. `git` not in $PATH.'));
    return false;
  }

  if (gitStatus === 'in-repo') {
    // In headless/CI mode, skip by default
    if (env.CI) {
      debug(chalk.dim('New project is already inside of a Git repo, skipping git init (CI mode).'));
      return false;
    }

    // In interactive mode, prompt the user
    const { answer } = await prompts({
      type: 'confirm',
      name: 'answer',
      message:
        'You are creating a project inside of an existing Git repository. Skip initializing a new git repository?',
      initial: true,
    });

    // If user confirms skip (or cancels/ctrl+c which returns undefined), skip git init
    if (answer !== false) {
      debug(chalk.dim('User chose to skip git init inside existing repo.'));
      return false;
    }
    // User explicitly chose not to skip, proceed with git init (creates nested repo)
    debug(chalk.dim('User chose to initialize git inside existing repo.'));
  }

  const packageJSON = require('../package.json');

  // not in git tree, so let's init
  try {
    await profileAsync('git:init', () =>
      spawnAsync('git', ['init'], { stdio: 'ignore', cwd: root })
    );
    await profileAsync('git:add', () =>
      spawnAsync('git', ['add', '-A'], { stdio: 'ignore', cwd: root })
    );

    const commitMsg = `Initial commit\n\nGenerated by ${packageJSON.name} ${packageJSON.version}.`;
    await profileAsync('git:commit', () =>
      spawnAsync('git', ['commit', '-m', commitMsg], {
        stdio: 'ignore',
        cwd: root,
      })
    );

    debug(chalk.dim('Initialized a Git repository.'));
    return true;
  } catch (error: any) {
    debug('Error initializing Git repo:', error);
    // no-op -- this is just a convenience and we don't care if it fails
    return false;
  }
}
