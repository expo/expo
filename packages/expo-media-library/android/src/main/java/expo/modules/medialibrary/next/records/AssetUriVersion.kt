package expo.modules.medialibrary.next.records

import expo.modules.kotlin.types.Enumerable

enum class AssetUriVersion(val value: String) : Enumerable {
  CURRENT("current"),
  ORIGINAL("original")
}
