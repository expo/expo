{
  "extends": "expo-module-scripts/tsconfig.base",
  "compilerOptions": {
    "composite": true,
    // `rootDir` spans both `src` and the hand-maintained `mocks/` module, which
    // types itself against `src/VideoPlayer.types`.
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["./src", "./mocks"]
}
