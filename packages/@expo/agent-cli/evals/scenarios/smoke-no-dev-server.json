{
  "id": "smoke-no-dev-server",
  "fixture": "e2e/fixtures/go-app",
  "taskPrompt": "check that this app builds and comes up",
  "drivingAgent": "deterministic",
  "tiers": [0],
  "command": {
    "argv": ["smoke", "--dev-server-url", "http://127.0.0.1:1", "--timeout", "1000"],
    "env": {
      "LOG_EVENTS": "events.jsonl"
    },
    "timeoutMs": 60000
  },
  "graders": [
    {
      "type": "exit-code",
      "expect": 20
    },
    {
      "type": "jsonl-event",
      "file": "events.jsonl",
      "event": "cli:smoke"
    }
  ]
}
