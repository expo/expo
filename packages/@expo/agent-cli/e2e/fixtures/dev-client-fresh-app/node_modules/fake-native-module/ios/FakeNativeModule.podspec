# Marker file: the presence of an `ios/` directory with a podspec is what makes this package
# look like a native module to the project-state probe. It is never used by CocoaPods here.
Pod::Spec.new do |s|
  s.name = 'FakeNativeModule'
  s.version = '1.0.0'
  s.platforms = { ios: '15.1' }
end
