---
name: Fake Module Usage
description: Use the fake module in an Expo app. Covers setup, the hook API, and common errors.
---

# Fake Module Usage

Install the package and read the config value.

```ts
import { getValue } from 'fake-module-with-skills';

const value = getValue();
```

Body of usage skill.
