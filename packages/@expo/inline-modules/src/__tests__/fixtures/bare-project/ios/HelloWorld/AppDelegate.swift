// Placeholder that marks `ios/HelloWorld/` as the app source root.
// `IOSConfig.Paths.getSourceRoot` locates the directory by globbing
// `ios/*/AppDelegate.@(m|mm|swift)`, and `getProjectName` returns its
// basename ("HelloWorld"). The tests rely on this to resolve the main
// target name from disk; the file content is never read.
