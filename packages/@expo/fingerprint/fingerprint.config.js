// const { SourceSkips } = require('@expo/fingerprint/build/Fingerprint.types');

/** @type {import('@expo/fingerprint').Config} */
const config = {
  hashAlgorithm: 'sha256',
};

console.log('ooxx', config);

module.exports = config;
