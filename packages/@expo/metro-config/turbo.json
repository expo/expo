{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["build/**"],
      "dependsOn": [
        "@expo/config#build",
        "@expo/env#build",
        "@expo/json-file#build"
      ]
    }
  }
}
