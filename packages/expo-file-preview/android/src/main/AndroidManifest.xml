<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <provider
      android:name=".FilePreviewFileProvider"
      android:authorities="${applicationId}.FilePreviewFileProvider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_preview_provider_paths" />
    </provider>
  </application>

  <queries>
    <intent>
      <!-- Required for preview handler checks when targeting API 30+. -->
      <action android:name="android.intent.action.VIEW" />
      <data android:mimeType="*/*" />
    </intent>
  </queries>
</manifest>
