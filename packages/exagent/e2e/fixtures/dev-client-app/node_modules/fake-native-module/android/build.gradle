// Marker file: the presence of an `android/` directory with a Gradle build file is what makes
// this package look like a native module to the project-state probe. Gradle never reads it here.
group = 'expo.modules.fake'
version = '1.0.0'
