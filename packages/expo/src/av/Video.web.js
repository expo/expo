// @flow

import React from 'react';
import { Text, View } from 'react-native';

export default class Video extends React.Component<{}> {
  render() {
    return (
      <View>
        <Text>Video Component not supported on the web</Text>
      </View>
    );
  }
}
