// @flow

import React from 'react';
import { Text, View } from 'react-native';

export default class Audio extends React.Component<{}> {
  render() {
    return (
      <View>
        <Text>Audio Component not supported on the web</Text>
      </View>
    );
  }
}
