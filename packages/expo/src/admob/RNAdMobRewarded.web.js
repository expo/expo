// @flow

import React from 'react';
import { Text, View } from 'react-native';

export default class RNAdMobRewarded extends React.Component<{}> {
  render() {
    return (
      <View>
        <Text>RNAdMobRewarded Component not supported on the web</Text>
      </View>
    );
  }
}
