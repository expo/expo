<!--
    Settings in this file should/will be overridden by specific app's resources.
-->
<resources>
  <style name="SplashScreenNoAnimation">
    <item name="android:windowEnterAnimation">@null</item>
    <item name="android:windowExitAnimation">@null</item>
  </style>

  <style name="SplashScreenFadeOutAnimation">
    <item name="android:windowEnterAnimation">@null</item>
    <item name="android:windowExitAnimation">@anim/fade_out</item>
  </style>


  <style name="Theme.Expo.SplashScreen" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Set the splash screen background, animated icon, and animation duration. -->
    <item name="windowSplashScreenBackground">#000020</item>
    <item name="android:opacity">opaque</item>

    <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
         animated drawable. One of these is required. -->
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_image</item>
    <!-- Set the theme of the Activity that directly follows your splash screen. -->
    <!-- Required -->
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>
