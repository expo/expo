require_relative '../../../../react-native-lab/react-native/scripts/autolink-ios.rb'

use_frameworks!

platform :ios, '10.0'

target 'EXUpdates_Example' do
  pod 'EXUpdates', :path => '../'
  pod 'UMCore', :path => '../../../@unimodules/core/ios'
  use_react_native! path: '../../../../react-native-lab/react-native'

  target 'EXUpdates_Tests' do
    inherit! :search_paths

    
  end
end
