import type { CollapsibleProps } from './types';
/**
 * A primitive that toggles visibility of its content via a labelled tappable
 * header. Controlled via `isOpen` + `onOpenChange`.
 */
export declare function Collapsible({ isOpen, onOpenChange, label, labelStyle, children, ref, }: CollapsibleProps): import("react/jsx-runtime").JSX.Element;
export * from './types';
//# sourceMappingURL=index.d.ts.map