#!/usr/bin/env node
'use strict';

const path = require('path');
const { commandRunner, getArgs } = require("../util/commandUtils");

process.env.EXPO_NONINTERACTIVE = 1;

commandRunner(path.join(__dirname, 'expo-module-tsc'), ['--noEmit', ...getArgs()]);