#!/usr/bin/env node
'use strict';

const { commandRunner, getArgs } = require("../util/commandUtils");

let args = getArgs();

if (!process.env.CI && !process.env.EXPO_NONINTERACTIVE) {
  args = [...args, '--watch']
}

commandRunner('yarn', ['tsc', ...args], true);