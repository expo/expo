#!/usr/bin/env node
'use strict';

import process from 'node:process';
import { npx } from './npx';

function expoModuleJest(args) {
  npx(['jest', ...args]);
}

if (import.meta.main) {
  expoModuleJest(process.argv);
}
