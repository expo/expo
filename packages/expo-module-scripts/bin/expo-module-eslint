#!/usr/bin/env node
'use strict';

import process from 'node:process';

import { npx } from './npx';

export function expoModuleEslint(args) {
  npx(['eslint', ...args]);
}

if (import.meta.main) {
  expoModuleEslint(process.argv);
}
