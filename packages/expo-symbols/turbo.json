{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["build/**"],
      "dependsOn": [
        "^build",
        "expo-constants#build"
      ]
    }
  }
}
