import type { NativeTabsScrollEdgeAppearanceProps } from './types';

/**
 * The component which can be used to customize the scroll edge appearance of the tab bar.
 * This appearance will be used when there is no scroll view or the root scroll view is fully expanded.
 *
 * > **Note:** You can use the alias `NativeTabs.ScrollEdgeAppearance` for this component.
 *
 * @platform ios
 */
export function NativeTabsScrollEdgeAppearance(props: NativeTabsScrollEdgeAppearanceProps) {
  return null;
}
