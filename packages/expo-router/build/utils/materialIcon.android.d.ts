import { type AndroidSymbol } from 'expo-symbols';
import type { ImageSourcePropType } from 'react-native';
/**
 * Resolves an Android Material Design icon name to an `ImageSourcePropType`.
 * Returns `undefined` while the icon is loading or if `name` is `undefined`.
 *
 * @platform android
 */
export declare function useMaterialIconSource(name: AndroidSymbol | undefined): ImageSourcePropType | undefined;
//# sourceMappingURL=materialIcon.android.d.ts.map