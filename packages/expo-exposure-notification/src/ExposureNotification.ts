import ExpoExposureNotification from './ExpoExposureNotification';
import { SampleOptions } from './ExposureNotification.types';


export * from './ExposureNotification.types';

/**
 * Great method that does a lot great stuff.
 * @param options specifies what great stuff you really want.
 *
 * @example
 * ```typescript
 * const result = await someGreatMethodAsync({ someOption: 'awesome' });
 * ```
 */
export async function someGreatMethodAsync(options: SampleOptions) {
  return await ExpoExposureNotification.someGreatMethodAsync(options);
}
