import * as ExposureNotification from '../ExposureNotification';

describe('ExposureNotification', () => {
  it('someGreatMethodAsync is defined', async () => {
    expect(ExposureNotification.someGreatMethodAsync).toBeDefined();
  });
});
