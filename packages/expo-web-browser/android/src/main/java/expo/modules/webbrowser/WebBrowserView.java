package expo.modules.webbrowser;

import android.content.Context;
import android.view.View;

import expo.core.ModuleRegistry;

public class WebBrowserView extends View {
  public WebBrowserView(Context context, ModuleRegistry moduleRegistry) {
    super(context);
  }
}
