import ExpoSystemNavigationBar from './ExpoSystemNavigationBar';
import { SampleOptions } from './SystemNavigationBar.types';


export * from './SystemNavigationBar.types';

/**
 * Great method that does a lot great stuff.
 * @param options specifies what great stuff you really want.
 *
 * @example
 * ```typescript
 * const result = await someGreatMethodAsync({ someOption: 'awesome' });
 * ```
 */
export async function someGreatMethodAsync(options: SampleOptions) {
  return await ExpoSystemNavigationBar.someGreatMethodAsync(options);
}
