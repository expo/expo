import * as SystemNavigationBar from '../SystemNavigationBar';

describe('SystemNavigationBar', () => {
  it('someGreatMethodAsync is defined', async () => {
    expect(SystemNavigationBar.someGreatMethodAsync).toBeDefined();
  });
});
