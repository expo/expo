const host = 'exp.host';
const origin = `https://${host}`;
const websiteOrigin = 'https://expo.io';

export default {
  api: {
    host,
    origin,
  },
  website: {
    origin: websiteOrigin,
  },
};
