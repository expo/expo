// Copyright 2025-present 650 Industries. All rights reserved.

// Minimal compilation unit kept so the ExpoModulesWorkletsAdapter pod
// validates with CocoaPods even when `react-native-worklets` is not
// installed — in that case every other file in this directory is
// excluded by the podspec and this one exists solely to give the pod a
// single non-empty source.
