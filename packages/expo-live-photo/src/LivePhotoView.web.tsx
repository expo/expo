import * as React from 'react';
import { View, Text } from 'react-native';

import { LivePhotoViewProps } from './LivePhoto.types';

export default function ExpoLivePhotoView(props: LivePhotoViewProps) {
  return (
    <View>
      <Text>expo-live-photo is not yet supported on web</Text>
    </View>
  );
}
