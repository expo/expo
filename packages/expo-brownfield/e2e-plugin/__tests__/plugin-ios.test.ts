describe('plugin for ios', () => {
  it('2 + 2 = 4', () => {
    expect(2 + 2).toBe(4);
  });

  // Test no values

  // Test bundle identifier

  // Test target name

  // Test bundle identifier + target name
});
