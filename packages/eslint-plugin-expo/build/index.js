"use strict";
const rules_1 = require("./rules");
const plugin = {
    meta: {
        name: 'eslint-plugin-expo',
        version: '0.0.1',
    },
    rules: rules_1.rules,
};
module.exports = plugin;
