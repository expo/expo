{
  "name": "expo-sqlite-skill-evals",
  "version": "0.0.0",
  "private": true,
  "description": "Anchors vitest's project root to this directory so `npx vitest run` works here without a local install."
}
