import { DocumentResult, GetDocumentOptions } from './types';
declare const _default: {
    readonly name: string;
    getDocumentAsync({ type, multiple, }: GetDocumentOptions): Promise<DocumentResult>;
};
export default _default;
