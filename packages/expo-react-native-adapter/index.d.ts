// A temporary type declaration until we convert this module to TypeScript, so this is rather bare
// for now

export class EventEmitter {
  constructor(nativeModule: any);
  [property: string]: any;
}

export const NativeModulesProxy: any;
export const Platform: any;
export const requireNativeViewManager: any;
