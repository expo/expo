extends: existence
message: "Remove the space at the start or end of the link text."
level: error
scope: raw
nonword: true
tokens:
  - '[^\s\[][ \t]+\]\('
  - '(?m)(?:^|[^!\w\]])\[[ \t]+[^\s\]][^\]\n]*\]\('
