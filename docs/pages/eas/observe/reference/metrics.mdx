---
title: Metrics reference
sidebar_title: Metrics
description: Detailed descriptions of each performance metric tracked by EAS Observe, including concepts and data handling.
---

## Concepts

### Session

A **session** starts when the app process is launched and ends when the app process is terminated. Each session has a unique identifier and contains all events that occurred during that app launch.

### User

A **user** is identified by an anonymous ID that is unique per app installation. This ID:

- Is generated when the app is first installed
- Persists across app updates
- Is reset if the user uninstalls and reinstalls the app
- Is not personally identifiable information (PII)

This allows you to see metrics across multiple sessions for the same user without collecting personal data.

## Metrics

### Cold Launch Time

Time to launch the app from scratch when the app's native code and resources are not in memory. This is a native-only metric that is measured before the JavaScript bundle starts executing.

A cold launch occurs after a fresh install, app upgrade, device reboot, or when the OS has killed the app to reclaim memory. The system must load the app from disk and create the process, making this the slowest launch type.

This metric is collected automatically.

### Warm Launch Time

Time to launch the app when native libraries, app's state, and resources are already cached in memory. This is a native-only metric that is measured before the JavaScript bundle starts executing.

During a warm launch, the system only needs to recreate the UI and restore any preserved state.

This metric is collected automatically.

### Bundle Load Time

How long it takes to evaluate your JavaScript bundle. This measures the time the JavaScript runtime spends parsing and executing your bundle code.

This metric is collected automatically.

### Time to First Render

Time from app launch until your app first renders content to the screen.

This metric is collected automatically when you wrap your root layout with `AppMetricsRoot` (see [Get started](/eas/observe/get-started/)). You can also call `AppMetrics.markFirstRender()` manually if needed.

### Time to Interactive

Time from app launch until your app is fully ready for user interaction. This is measured when you call `AppMetrics.markInteractive()`.

**When to call:** After all initialization work is complete and the splash screen is hidden. This should represent the moment when users can actually start using your app.

`markInteractive()` can safely be called multiple times per session, only the first call records the measurement. Call it on every screen that users can deep link to. If you only place it on one screen, TTI will be measured incorrectly when the app is opened via a deep link to a different screen: for example, if screen A has the call but the user deep links to screen B, TTI will not be recorded until they navigate to A.

## Data handling

### Offline collection

Metrics collected while the device is offline are stored locally on the device. They are automatically sent to the server when the app moves to the background and there is connectivity. You can also call `ExpoObserve.dispatchEvents()` to flush events manually at any time (see [Configuration](/eas/observe/configuration/)).

### Data retention

Metric data is retained for a minimum of 60 days.

### Sampling

All sessions are tracked. There is no sampling applied to the data collection.

### Environment

All metrics are grouped by environment. The environment value is derived from `process.env.NODE_ENV` by default, or can be overridden via `configure({ environment })`. Metrics with the `development` environment are filtered out before dispatching unless `enableInDebug` is set to `true` in your app config (see [Enable metrics in development](/eas/observe/configuration/#enable-metrics-in-development)). You can also disable all dispatching globally using `configure({ dispatchingEnabled: false })`.
