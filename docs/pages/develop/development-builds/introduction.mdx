import React from 'react';
import {
  View,
  Text,
  ScrollView,
  StyleSheet,
  Image,
  TouchableOpacity,
  Linking,
} from 'react-native';

export default function App() {
  const services = [
    {
      title: 'Banner Printing',
      price: '₹25/sqft',
      image:
        'https://images.unsplash.com/photo-1520607162513-77705c0f0d4a?q=80&w=1200&auto=format&fit=crop',
    },
    {
      title: 'Invitation Cards',
      price: '₹5/card',
      image:
        'https://images.unsplash.com/photo-1511795409834-ef04bbd61622?q=80&w=1200&auto=format&fit=crop',
    },
    {
      title: 'Flex & Poster',
      price: 'Best Quality',
      image:
        'https://images.unsplash.com/photo-1504384308090-c894fdcc538d?q=80&w=1200&auto=format&fit=crop',
    },
    {
      title: 'Visiting Cards',
      price: '₹150/100 pcs',
      image:
        'https://images.unsplash.com/photo-1556740749-887f6717d7e4?q=80&w=1200&auto=format&fit=crop',
    },
  ];

  const openWhatsApp = () => {
    Linking.openURL('https://wa.me/919451100151');
  };

  return (
    <ScrollView style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.title}>Chauhan Printing Press</Text>
        <Text style={styles.subtitle}>
          Banner • Flex • Invitation Card • Poster • ID Card
        </Text>

        <View style={styles.callBox}>
          <Text style={styles.callText}>Call Now</Text>
          <Text style={styles.phone}>9451100151</Text>
        </View>
      </View>

      {services.map((service, index) => (
        <View key={index} style={styles.card}>
          <Image source={{ uri: service.image }} style={styles.image} />

          <View style={styles.cardContent}>
            <Text style={styles.serviceTitle}>{service.title}</Text>
            <Text style={styles.price}>{service.price}</Text>

            <TouchableOpacity style={styles.button} onPress={openWhatsApp}>
              <Text style={styles.buttonText}>Order Now</Text>
            </TouchableOpacity>
          </View>
        </View>
      ))}

      <View style={styles.featureBox}>
        <Text style={styles.featureTitle}>Fast Delivery</Text>
        <Text style={styles.featureText}>
          Same day printing available for urgent work.
        </Text>
      </View>

      <View style={styles.featureBox}>
        <Text style={styles.featureTitle}>HD Quality</Text>
        <Text style={styles.featureText}>
          High quality print with modern machines.
        </Text>
      </View>

      <View style={styles.featureBox}>
        <Text style={styles.featureTitle}>Custom Design</Text>
        <Text style={styles.featureText}>
          Wedding cards, political banners and all graphics design.
        </Text>
      </View>

      <TouchableOpacity style={styles.whatsappBox} onPress={openWhatsApp}>
        <Text style={styles.whatsappTitle}>Place Your Order on WhatsApp</Text>
        <Text style={styles.whatsappText}>WhatsApp: 9451100151</Text>
      </TouchableOpacity>

      <Text style={styles.footer}>
        Developed for Chauhan Printing Press • Munshiganj, Amethi
      </Text>

      <View style={{ height: 40 }} />
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#111827',
    padding: 15,
  },
  header: {
    backgroundColor: '#7f1d1d',
    borderRadius: 25,
    padding: 20,
    marginBottom: 20,
  },
  title: {
    fontSize: 30,
    color: '#facc15',
    fontWeight: 'bold',
  },
  subtitle: {
    color: '#ffffff',
    marginTop: 8,
    fontSize: 15,
  },
  callBox: {
    backgroundColor: '#facc15',
    padding: 15,
    borderRadius: 20,
    marginTop: 20,
  },
  callText: {
    color: '#000',
    fontSize: 16,
    fontWeight: 'bold',
  },
  phone: {
    color: '#000',
    fontSize: 24,
    fontWeight: 'bold',
  },
  card: {
    backgroundColor: '#1f2937',
    borderRadius: 25,
    overflow: 'hidden',
    marginBottom: 20,
  },
  image: {
    width: '100%',
    height: 220,
  },
  cardContent: {
    padding: 15,
  },
  serviceTitle: {
    color: '#fde047',
    fontSize: 24,
    fontWeight: 'bold',
  },
  price: {
    color: '#fff',
    marginTop: 8,
    fontSize: 18,
  },
  button: {
    backgroundColor: '#dc2626',
    padding: 14,
    borderRadius: 18,
    marginTop: 15,
    alignItems: 'center',
  },
  buttonText: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 18,
  },
  featureBox: {
    backgroundColor: '#1f2937',
    padding: 20,
    borderRadius: 25,
    marginBottom: 15,
  },
  featureTitle: {
    color: '#fde047',
    fontSize: 22,
    fontWeight: 'bold',
  },
  featureText: {
    color: '#fff',
    marginTop: 8,
    fontSize: 16,
  },
  whatsappBox: {
    backgroundColor: '#facc15',
    padding: 25,
    borderRadius: 25,
    marginTop: 20,
    alignItems: 'center',
  },
  whatsappTitle: {
    color: '#000',
    fontSize: 26,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  whatsappText: {
    color: '#000',
    fontSize: 20,
    marginTop: 10,
    fontWeight: 'bold',
  },
  footer: {
    color: '#9ca3af',
    textAlign: 'center',
    marginTop: 25,
  },
});

/*
APK BUILD STEPS

1. Install Node.js
2. Run command:
   npm install -g expo-cli

3. Create Expo App:
   npx create-expo-app printing-app

4. Replace App.js with this code.

5. Run App:
   npm start

6. Build APK:
   npx eas build -p android --profile preview

7. Download APK from Expo build link.
*/
