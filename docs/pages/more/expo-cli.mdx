npm install -g expo-cli
import React, { useState, useEffect } from 'react'; import { View, Text, Image, Animated, Easing, useColorScheme } from 'react-native'; import AsyncStorage from '@react-native-async-storage/async-storage'; import { Audio } from 'expo-av';

const APP_NAME = 'Toli Me'; const APP_CREATOR = 'Conçu par O'Neal Chauncey AMBAH'; const LOGO_URI = 'https://example.com/toli-me-logo.png'; // Remplace par l'URL de ton logo const WELCOME_SOUND_URI = 'https://example.com/welcome-sound.mp3'; // Remplace par l'URL du son const DEFAULT_THEME_COLORS = { light: '#007AFF', dark: '#FFC107', custom: ['#008000', '#FFD700', '#0000FF'] };

const SplashScreen = ({ onFinish }) => { const fadeAnim = useState(new Animated.Value(0))[0]; const slideAnim = useState(new Animated.Value(50))[0];

useEffect(() => { const playWelcomeSound = async () => { const { sound } = await Audio.Sound.createAsync({ uri: WELCOME_SOUND_URI }); await sound.playAsync(); }; playWelcomeSound();

Animated.parallel([
  Animated.timing(fadeAnim, {
    toValue: 1,
    duration: 2000,
    easing: Easing.ease,
    useNativeDriver: true,
  }),
  Animated.timing(slideAnim, {
    toValue: 0,
    duration: 2000,
    easing: Easing.out(Easing.exp),
    useNativeDriver: true,
  })
]).start(() => {
  setTimeout(onFinish, 1000);
});

}, []);

return ( <Animated.View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#008000', opacity: fadeAnim, transform: [{ translateY: slideAnim }] }}> <Image source={{ uri: LOGO_URI }} style={{ width: 150, height: 150 }} /> <Text style={{ fontSize: 24, fontWeight: 'bold', color: 'white', marginTop: 20 }}>{APP_NAME}</Text> <Text style={{ fontSize: 14, color: 'white', marginTop: 10 }}>{APP_CREATOR}</Text> </Animated.View> ); };

const App = () => { const [isSplashFinished, setIsSplashFinished] = useState(false); const fadeAnim = useState(new Animated.Value(0))[0]; const colorScheme = useColorScheme(); const isDarkMode = colorScheme === 'dark';

useEffect(() => { if (isSplashFinished) { Animated.timing(fadeAnim, { toValue: 1, duration: 1000, easing: Easing.ease, useNativeDriver: true, }).start(); } }, [isSplashFinished]);

return ( isSplashFinished ? ( <Animated.View style={{ flex: 1, backgroundColor: isDarkMode ? '#121212' : '#FFFFFF', justifyContent: 'center', alignItems: 'center', opacity: fadeAnim }}> <Text style={{ fontSize: 18, fontWeight: 'bold', color: isDarkMode ? 'white' : 'black' }}>Bienvenue sur {APP_NAME}</Text> <Text style={{ fontSize: 14, color: isDarkMode ? 'white' : 'black', marginTop: 10 }}>{APP_CREATOR}</Text> </Animated.View> ) : ( <SplashScreen onFinish={() => setIsSplashFinished(true)} /> ) ); };

export default App;
npm install
expo start
