import React from 'react';
import { View, Text, StyleSheet, Button, SafeAreaView, ScrollView } from 'react-native';
import { LineChart } from 'react-native-chart-kit';
import { Dimensions } from 'react-native';

const screenWidth = Dimensions.get("window").width;

const App = () => {
  const tradeData = {
    labels: ['10:00', '11:00', '12:00', '13:00', '14:00'],
    datasets: [
      {
        data: [50, 100, 80, 120, 90]
      }
    ]
  };

  return (
    <SafeAreaView style={styles.container}>
      <ScrollView contentContainerStyle={styles.scrollContent}>
        <Text style={styles.header}>Trading Dashboard</Text>
        
        <View style={styles.card}>
          <Text style={styles.cardTitle}>Current Trade</Text>
          <Text style={styles.infoText}>Status: <Text style={styles.tradeStatus}>BUY</Text></Text>
          <Text style={styles.infoText}>Take Profit: 1.2345</Text>
          <Text style={styles.infoText}>Stop Loss: 1.2300</Text>
        </View>
        
        <View style={styles.card}>
          <Text style={styles.cardTitle}>Trade History</Text>
          <LineChart
            data={tradeData}
            width={screenWidth - 40} // Adjust width for padding
            height={200}
            yAxisLabel="$"
            chartConfig={{
              backgroundColor: '#ffffff',
              backgroundGradientFrom: '#ffffff',
              backgroundGradientTo: '#ffffff',
              decimalPlaces: 0,
              color: (opacity = 1) => `rgba(74, 144, 226, ${opacity})`,
              labelColor: (opacity = 1) => `rgba(74, 144, 226, ${opacity})`,
              style: {
                borderRadius: 16
              },
              propsForDots: {
                r: "6",
                strokeWidth: "2",
                stroke: "#4A90E2"
              }
            }}
            style={{
              marginVertical: 8,
              borderRadius: 16
            }}
          />
        </View>
        
        <View style={styles.buttonContainer}>
          <Button title="Place Trade" color="#4A90E2" onPress={() => { /* Trading logic here */ }} />
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#E6F0FA'
  },
  scrollContent: {
    padding: 16,
    alignItems: 'center'
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#4A90E2',
    marginBottom: 20,
    textAlign: 'center'
  },
  card: {
    backgroundColor: '#FFFFFF',import React from 'react';
import { View, Text, StyleSheet, Button, SafeAreaView, ScrollView } from 'react-native';
import { LineChart } from 'react-native-chart-kit';
import { Dimensions } from 'react-native';

const screenWidth = Dimensions.get("window").width;

const App = () => {
  const tradeData = {
    labels: ['10:00', '11:00', '12:00', '13:00', '14:00'],
    datasets: [
      {
        data: [50, 100, 80, 120, 90]
      }
    ]
  };

  return (
    <SafeAreaView style={styles.container}>
      <ScrollView contentContainerStyle={styles.scrollContent}>
        <Text style={styles.header}>Trading Dashboard</Text>
        
        <View style={styles.card}>
          <Text style={styles.cardTitle}>Current Trade</Text>
          <Text style={styles.infoText}>Status: <Text style={styles.tradeStatus}>BUY</Text></Text>
          <Text style={styles.infoText}>Take Profit: 1.2345</Text>
          <Text style={styles.infoText}>Stop Loss: 1.2300</Text>
        </View>
        
        <View style={styles.card}>
          <Text style={styles.cardTitle}>Trade History</Text>
          <LineChart
            data={tradeData}
            width={screenWidth - 40} // Adjust width for padding
            height={200}
            yAxisLabel="$"
            chartConfig={{
              backgroundColor: '#ffffff',
              backgroundGradientFrom: '#ffffff',
              backgroundGradientTo: '#ffffff',
              decimalPlaces: 0,
              color: (opacity = 1) => `rgba(74, 144, 226, ${opacity})`,
              labelColor: (opacity = 1) => `rgba(74, 144, 226, ${opacity})`,
              style: {
                borderRadius: 16
              },
              propsForDots: {
                r: "6",
                strokeWidth: "2",
                stroke: "#4A90E2"
              }
            }}
            style={{
              marginVertical: 8,
              borderRadius: 16
            }}
          />
        </View>
        
        <View style={styles.buttonContainer}>
          <Button title="Place Trade" color="#4A90E2" onPress={() => { /* Trading logic here */ }} />
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#E6F0FA'
  },
  scrollContent: {
    padding: 16,
    alignItems: 'center'
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#4A90E2',
    marginBottom: 20,
    textAlign: 'center'
  },
  card: {
    backgroundColor: '#FFFFFF',
    width: '100%',
    padding: 16,
    borderRadius: 16,
    marginBottom: 20,
    shadowColor: "#000",
    shadowOffset: {
     width: 0,
     height: 2,
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 5,
  },
  cardTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#4A90E2',
 marginBottom: 8
  },
  infoText: {
    fontSize: 16,
    color: '#333'
  },
  tradeStatus: {
    color: 'green',
    fontWeight: 'bold'
  },
  buttonContainer: {
    width: '100%',
    marginTop: 10
  }
});

export default App;￼Enter
