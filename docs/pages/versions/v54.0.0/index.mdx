import React from 'react';
import { View, Text, StyleSheet, ScrollView, TouchableOpacity, Image } from 'react-native';

const ApexWinHome = () => {
  // चुनिंदा 11 गेम्स की लिस्ट
  const selectedGames = [
    { id: 1, name: 'Win Go 1Min', category: 'Lottery', icon: '🎰' },
    { id: 2, name: 'Aviator', category: 'Hot', icon: '✈️' },
    { id: 3, name: 'Chicken Road 2', category: 'Hot', icon: '🐔' },
    { id: 4, name: 'Mines Pro', category: 'Original', icon: '💣' },
    { id: 5, name: '7Up 7Down', category: 'Cards', icon: '🎲' },
    { id: 6, name: 'Squid Gambler', category: 'Hot', icon: '🦑' },
    { id: 7, name: 'Dragon vs Tiger', category: 'Cards', icon: '🐉' },
    { id: 8, name: 'Fortune Gems', category: 'Slots', icon: '💎' },
    { id: 9, name: 'Plinko', category: 'Original', icon: '⚪' },
    { id: 10, name: 'Rummy', category: 'Casino', icon: '🃏' },
    { id: 11, name: 'K3 Lottery', category: 'Lottery', icon: '🔢' },
  ];

  return (
    <ScrollView style={styles.container}>
      {/* Header & Wallet Section */}
      <View style={styles.header}>
        <Text style={styles.logoText}>Apex<Text style={{color: '#F59E0B'}}>Win</Text></Text>
        <View style={styles.walletBox}>
          <Text style={styles.balanceText}>Balance: ₹1,000.00</Text>
          <TouchableOpacity style={styles.depositBtn}>
            <Text style={styles.btnText}>+ Deposit</Text>
          </TouchableOpacity>
        </View>
      </View>

      {/* VIP & Bonus Banner */}
      <View style={styles.banner}>
        <Text style={styles.bannerTitle}>VIP 2 Privilege Unlocked</Text>
        <Text style={styles.bannerSub}>Get daily rebate & attendance bonus</Text>
      </View>

      {/* Selected Games Section */}
      <Text style={styles.sectionTitle}>Featured Games</Text>
      <View style={styles.gameGrid}>
        {selectedGames.map((game) => (
          <TouchableOpacity key={game.id} style={styles.gameCard}>
            <Text style={{fontSize: 32}}>{game.icon}</Text>
            <Text style={styles.gameName}>{game.name}</Text>
            <Text style={styles.gameCategory}>{game.category}</Text>
          </TouchableOpacity>
        ))}
      </View>
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#0F172A', padding: 15 },
  header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginVertical: 10 },
  logoText: { fontSize: 24, fontWeight: 'bold', color: '#FFF' },
  walletBox: { backgroundColor: '#1E293B', padding: 10, borderRadius: 8, alignItems: 'center' },
  balanceText: { color: '#10B981', fontWeight: 'bold' },
  depositBtn: { backgroundColor: '#F59E0B', paddingHorizontal: 12, paddingVertical: 5, borderRadius: 5, marginTop: 5 },
  btnText: { color: '#000', fontWeight: 'bold' },
  banner: { backgroundColor: 'linear-gradient(135deg, #6366F1, #A855F7)', padding: 15, borderRadius: 12, marginVertical: 15 },
  bannerTitle: { color: '#FFF', fontSize: 18, fontWeight: 'bold' },
  bannerSub: { color: '#E2E8F0', fontSize: 12 },
  sectionTitle: { color: '#FFF', fontSize: 18, fontWeight: 'bold', marginBottom: 10 },
  gameGrid: { flexDirection: 'row', flexWrap: 'wrap', justifyContent: 'space-between' },
  gameCard: { backgroundColor: '#1E293B', width: '30%', padding: 10, borderRadius: 10, alignItems: 'center', marginBottom: 15 },
  gameName: { color: '#FFF', fontSize: 12, fontWeight: 'bold', marginTop: 5, textAlign: 'center' },
  gameCategory: { color: '#94A3B8', fontSize: 10 }
});

export default ApexWinHome;
