---
title: FloatingActionButton
description: A Jetpack Compose FloatingActionButton component following Material Design 3.
sourceCodeUrl: 'https://github.com/expo/expo/tree/main/packages/expo-ui'
packageName: '@expo/ui'
platforms: ['android']
---

import APISection from '~/components/plugins/APISection';
import { APIInstallSection } from '~/components/plugins/InstallSection';

Expo UI `FloatingActionButton` wraps Material Design 3's [`FloatingActionButton`](https://developer.android.com/develop/ui/compose/components/fab) and [`ExtendedFloatingActionButton`](https://developer.android.com/develop/ui/compose/components/fab#extended). When `label` is absent a standard FAB is rendered; when `label` is present an `ExtendedFloatingActionButton` with animated label expansion is rendered.

## Installation

<APIInstallSection />

## Usage

### Standard FAB

```tsx StandardFABExample.tsx
import { FloatingActionButton, Host, Icon } from '@expo/ui/jetpack-compose';

export default function StandardFABExample() {
  return (
    <Host matchContents>
      <FloatingActionButton onPress={() => console.log('FAB pressed')}>
        <Icon source={require('./assets/add.xml')} />
      </FloatingActionButton>
    </Host>
  );
}
```

### FAB sizes

```tsx FABSizesExample.tsx
import { FloatingActionButton, Host, Icon } from '@expo/ui/jetpack-compose';
import { View } from 'react-native';

export default function FABSizesExample() {
  return (
    <View style={{ flexDirection: 'row', gap: 16 }}>
      <Host matchContents>
        <FloatingActionButton size="small" onPress={() => {}}>
          <Icon source={require('./assets/add.xml')} />
        </FloatingActionButton>
      </Host>
      <Host matchContents>
        <FloatingActionButton size="medium" onPress={() => {}}>
          <Icon source={require('./assets/add.xml')} />
        </FloatingActionButton>
      </Host>
      <Host matchContents>
        <FloatingActionButton size="large" onPress={() => {}}>
          <Icon source={require('./assets/add.xml')} />
        </FloatingActionButton>
      </Host>
    </View>
  );
}
```

### Extended FAB

```tsx ExtendedFABExample.tsx
import { FloatingActionButton, Host, Icon } from '@expo/ui/jetpack-compose';
import { useState } from 'react';

export default function ExtendedFABExample() {
  const [expanded, setExpanded] = useState(true);

  return (
    <Host matchContents>
      <FloatingActionButton
        label="Edit"
        expanded={expanded}
        onPress={() => setExpanded((v) => !v)}>
        <Icon source={require('./assets/edit.xml')} />
      </FloatingActionButton>
    </Host>
  );
}
```

### Custom color

```tsx FABCustomColorExample.tsx
import { FloatingActionButton, Host, Icon } from '@expo/ui/jetpack-compose';

export default function FABCustomColorExample() {
  return (
    <Host matchContents>
      <FloatingActionButton
        label="New item"
        containerColor="#E8DEF8"
        onPress={() => console.log('pressed')}>
        <Icon source={require('./assets/add.xml')} />
      </FloatingActionButton>
    </Host>
  );
}
```

## API

```tsx
import { FloatingActionButton } from '@expo/ui/jetpack-compose';
```

<APISection packageName="expo-ui/jetpack-compose/floatingactionbutton" apiName="FloatingActionButton" />
