Understood. You want everything in one structured list, in the exact order you will follow — with numbers, file names, and what to paste where.


---

🚧 CONTRACTOR ALLY - STEP-BY-STEP SETUP (ALL IN ONE PLACE)


---

1️⃣ RUN THESE COMMANDS IN TERMINAL

npx create-expo-app@latest contractor-ally --template default
cd contractor-ally
npm i @tanstack/react-query axios zustand zod react-hook-form
npm i expo-secure-store expo-file-system expo-image-picker expo-notifications
npm i react-native-paper react-native-safe-area-context react-native-vector-icons
npm i -D @types/react @types/react-native


---

2️⃣ CREATE THIS FILE IN ROOT FOLDER

📄 File Name: app.config.ts

export default {
  expo: {
    name: "Contractor Ally",
    slug: "contractor-ally",
    scheme: "contractorally",
    runtimeVersion: { policy: "sdkVersion" },
    platforms: ["ios","android","web"],
    plugins: ["expo-secure-store","expo-notifications","expo-image-picker"],
    android: { package: "com.yourcompany.contractorally" },
    ios: { bundleIdentifier: "com.yourcompany.contractorally" }
  }
}


---

3️⃣ CREATE FOLDERS

Create these folders inside your project:

app/
  (tabs)/
  auth/
lib/
  forms/
components/

Command version:

mkdir -p app/(tabs) app/auth lib/forms components/UI components/items


---

4️⃣ CREATE THESE FILES AND PASTE CODE


---

📁 Folder: lib

4.1 lib/api.ts

import axios from "axios";
export const api = axios.create({
  baseURL: process.env.EXPO_PUBLIC_API_URL || "http://localhost:54321/functions/v1",
  timeout: 15000
});

4.2 lib/queryClient.ts

import { QueryClient } from "@tanstack/react-query";
export const queryClient = new QueryClient();

4.3 lib/store.ts

import { create } from "zustand";

type Role = "owner" | "engineer" | "supervisor" | "contractor" | "admin";

type SessionState = {
  token?: string;
  role?: Role;
  setSession: (t?: string, r?: Role) => void;
  clear: () => void;
};

export const useSession = create<SessionState>((set) => ({
  token: undefined,
  role: undefined,
  setSession: (t, r) => set({ token: t, role: r }),
  clear: () => set({ token: undefined, role: undefined })
}));


---

📁 Folder: lib/forms

4.4 lib/forms/quote.ts

import { z } from "zod";

export const quoteItemSchema = z.object({
  description: z.string().min(2),
  qty: z.number().positive(),
  unit: z.string().optional(),
  rate: z.number().nonnegative()
});

export const quoteSchema = z.object({
  job_id: z.string().uuid(),
  contractor_id: z.string().uuid(),
  items: z.array(quoteItemSchema).min(1),
  discount: z.number().nonnegative().default(0),
  vat: z.number().nonnegative().default(0)
}).transform(v => {
  const subtotal = v.items.reduce((s,i)=> s + i.qty * i.rate, 0);
  const total = subtotal - v.discount + v.vat;
  return { ...v, subtotal, total };
});


---

📁 Folder: app/(tabs)

4.5 app/(tabs)/_layout.tsx

import { Tabs } from "expo-router";
export default function Layout() {
  return (
    <Tabs>
      <Tabs.Screen name="index" options={{ title: "Dashboard" }} />
      <Tabs.Screen name="jobs" options={{ title: "Jobs" }} />
      <Tabs.Screen name="quotes" options={{ title: "Quotes" }} />
      <Tabs.Screen name="lpos" options={{ title: "LPOs" }} />
      <Tabs.Screen name="invoices" options={{ title: "Invoices" }} />
      <Tabs.Screen name="contractors" options={{ title: "Contractors" }} />
      <Tabs.Screen name="reports" options={{ title: "Reports" }} />
    </Tabs>
  );
}

4.6 app/(tabs)/jobs.tsx

import { View, Text, FlatList } from "react-native";
import { useQuery } from "@tanstack/react-query";
import { api } from "@/lib/api";

export default function Jobs() {
  const { data, isLoading, error } = useQuery({
    queryKey: ["jobs"],
    queryFn: async () => (await api.get("/jobs")).data
  });

  if (isLoading) return <Text>Loading…</Text>;
  if (error) return <Text>Failed to load.</Text>;

  return (
    <FlatList
      data={data}
      keyExtractor={(item) => item.id}
      renderItem={({ item }) => (
        <View style={{ padding: 12 }}>
          <Text>{item.title}</Text>
          <Text>{item.status}</Text>
        </View>
      )}
    />
  );
}

(You will later create similar files for quotes.tsx, lpos.tsx, etc.)


---

5️⃣ CREATE ENVIRONMENT FILE

📄 File: .env (root folder)

EXPO_PUBLIC_API_URL=http://localhost:54321/functions/v1


---

6️⃣ START THE APP

npm run start


---

✅ NEXT STEP

Tell me which backend you prefer:

(A) Supabase (recommended)

(B) PocketBase (offline file)

(C) You already have your own API


So I can generate API endpoints and full data flow next.
