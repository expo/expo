---
title: Router UI
description: An Expo Router submodule that provides headless UI navigators to create custom layouts.
sourceCodeUrl: 'https://github.com/expo/expo/tree/main/packages/expo-router'
packageName: 'expo-router-ui'
platforms: ['android', 'ios', 'web']
isNew: true
---

import APISection from '~/components/plugins/APISection';
import { APIInstallSection } from '~/components/plugins/InstallSection';
import { ConfigPluginExample } from '~/ui/components/ConfigSection';

`expo-router/ui` is a submodule of `expo-router` library and exports components and hooks to build custom layouts, rather than using the default [React Navigation](https://reactnavigation.org/) navigators provided by `expo-router`.

> See the [Expo Router](./expo-router) reference for more information about the file-based routing library for native and web app.

## Installation

<APIInstallSection packageName="expo-router" hideBareInstructions />

## Configuration in app config

If you are using the [default](/more/create-expo/#--template) template to create a new project, `expo-router` [config plugin](/config-plugins/introduction/) is automatically configured in the app config automatically.

<ConfigPluginExample>

```json app.json
{
  "expo": {
    "plugins": ["expo-router"]
  }
}
```

</ConfigPluginExample>

## Usage

Find more information about using `expo-router/ui` in [Expo Router UI](/router/ui/tabs/) guide.

## API

```js
import { Tabs, TabList, TabTrigger, TabSlot } from 'expo-router/ui';
```

<APISection packageName="expo-router-ui" apiName="Tabs" />
