import React from "react"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Bell, Calendar, Home, BarChart3, Settings } from "lucide-react"; import { Progress } from "@/components/ui/progress";

export default function StudentTimetableApp() { const timetable = [ { subject: "Mathematics", time: "9:00 AM" }, { subject: "English", time: "10:00 AM" }, { subject: "Physics", time: "1:00 PM" }, { subject: "Chemistry", time: "3:00 PM" }, ];

return ( <div className="min-h-screen bg-white text-black flex flex-col justify-between"> {/* Header */} <div className="bg-blue-600 text-white px-4 py-3 flex justify-between items-center rounded-b-2xl shadow-md"> <h1 className="text-xl font-bold">Student Timetable</h1> <Bell className="w-6 h-6" /> </div>

{/* Timetable Section */}
  <div className="px-4 py-6">
    <h2 className="text-lg font-semibold mb-4">Timetable</h2>
    <div className="space-y-3">
      {timetable.map((entry, index) => (
        <Card key={index}>
          <CardContent className="flex justify-between items-center p-4">
            <span>{entry.subject}</span>
            <span className="text-sm text-gray-600">{entry.time}</span>
          </CardContent>
        </Card>
      ))}
    </div>
    <Button className="mt-4 w-full bg-blue-600 hover:bg-blue-700">+ Add Subject</Button>
  </div>

  {/* Daily Progress */}
  <div className="px-4 pb-6">
    <h2 className="text-lg font-semibold mb-2">Daily Progress</h2>
    <div className="flex items-center gap-4">
      <Progress value={75} className="w-full" />
      <span className="text-lg font-bold">75%</span>
    </div>
  </div>

  {/* Bottom Navigation */}
  <div className="bg-gray-100 py-3 px-6 flex justify-between items-center rounded-t-2xl shadow-inner">
    <Home className="w-6 h-6 text-gray-600" />
    <Calendar className="w-6 h-6 text-blue-600" />
    <BarChart3 className="w-6 h-6 text-gray-600" />
    <Settings className="w-6 h-6 text-gray-600" />
  </div>
</div>

); }

