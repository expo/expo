---
title: Manage different app versions
sidebar_title: Manage app versions
description: Learn about developer-facing and user-facing app versions and how EAS Build automatically manages developer-facing versions.
hasVideoLink: true
---

import { Collapsible } from '~/ui/components/Collapsible';
import { ProgressTracker } from '~/ui/components/ProgressTracker';
import { Terminal } from '~/ui/components/Snippet';
import { VideoBoxLink } from '~/ui/components/VideoBoxLink';

In this chapter, we'll learn how EAS Build automatically manages the developer-facing app version for Android and iOS. Learning about it will be useful before we dive into production build in the next two chapters.

<VideoBoxLink
  videoId="C8x4N9UmzS8"
  title="Watch: Automating app version code"
  description="Understand developer-facing and user-facing app versions and how EAS Build automates version management for you."
/>

---

## Understanding developer-facing and user-facing app versions

An app version is composed of two values:

- Developer-facing value: Represented by [`versionCode`](/versions/latest/config/app/#versioncode) for Android and [`buildNumber`](/versions/latest/config/app/#buildnumber) for iOS.
- User-facing value: Represented by [`version`](/versions/latest/config/app/#version) **app.config.js**.

Both Google Play Store and Apple App Store rely on developer-facing values to identify each unique build. For example, if we upload an app with the app version `1.0.0 (1)` (which is a combination of user-facing and developer-facing values), we cannot submit another build to the app stores with the same app version. Submitting builds with duplicate app version numbers results in a failed submission.

An example demonstration of manually managing developer-facing values is shown below by `android.versionCode` and `ios.buildNumber` in **app.config.js**. **We don't have to add or manage these values manually since EAS Build automates this for us**.

```js app.config.js
{
  ios: {
    buildNumber: 1
    /* @hide ... */ /* @end */
  },
  android: {
    versionCode: 1
  }
  /* @hide ... */ /* @end */
}
```

> **Note**: The [user-facing version number](/build-reference/app-versions/#user-facing-version) is not handled by EAS. Instead, we define that in the app store developer portals before submitting our production app for review.

## Automatic app version management with EAS Build

By default, EAS Build assists in automating developer-facing values. It utilizes the [remote version source](/build-reference/app-versions/#remote-version-source) to automatically increment developer-facing values whenever a new production release is made.

When we initialized the project with `eas init` command, the EAS CLI automatically added the following properties in **eas.json**:

- `cli.appVersionSource` which is set to `remote`
- [`build.production.autoIncrement`](/eas/json/#autoincrement-1) which is set to `true`

You can view them in your project's **eas.json**:

{/* prettier-ignore */}
```json eas.json
{
  "cli": {
    /* @hide ... */ /* @end */
    /* @info The `appVersionSource` is set to `remote`. */
    "appVersionSource": "remote"
    /* @end */
  },
  "build": {
    "production": {
      /* @info The `autoIncrement` is set `true` to automatically increment the `versionCode` or `buildNumber`. */
      "autoIncrement": true
      /* @end */
    }
  }
  /* @hide ... */ /* @end */
}
```

When we create a new production build in the next two chapters, the `versionCode` for Android and `buildNumber` for iOS will increment automatically.

<Collapsible summary="Syncing developer-facing app versions for already published apps to EAS">

If your app is already published in the app stores, the developer-facing app versions are already set. When migrating this app to use EAS Build, follow the steps below to sync those app versions:

- In the terminal window, run the `eas build:version:set` command:

<Terminal cmd={['$ eas build:version:set']} />

- Select the platform (Android or iOS) when prompted.
- When prompted **Do you want to set app version source to remote now?**, select **yes**. This will set the `cli.appVersionSource` to `remote` in **eas.json**.
- When prompted **What version would you like to initialize it with?**, enter the last version number that you have set in the app stores.

After these steps, the app versions will be synced to EAS Build remotely. You can set `build.production.autoIncrement` to `true` in **eas.json**. When you create a new production build, the `versionCode` and `buildNumber` will be automatically incremented from now on.

</Collapsible>

## Summary

<ProgressTracker name="EAS_TUTORIAL" currentChapterSlug="/tutorial/eas/manage-app-versions" />

const seedArticles = [
  {id:1,title:"دليل استقبال الموظف الجديد",category:"الموارد البشرية",type:"دليل",summary:"خطوات موحدة لاستقبال الموظفين الجدد وتجهيز حساباتهم وتعريفهم بالسياسات.",tags:["تهيئة","موظفون"],author:"نورة العتيبي",date:"منذ ساعتين",favorite:true},
  {id:2,title:"إجراء اعتماد طلبات الشراء",category:"العمليات",type:"إجراء",summary:"المسار المعتمد لرفع طلبات الشراء ومراجعتها والموافقة عليها حسب الصلاحيات.",tags:["مشتريات","اعتمادات"],author:"خالد الحربي",date:"أمس",favorite:false},
  {id:3,title:"سياسة أمن كلمات المرور",category:"التقنية",type:"سياسة",summary:"متطلبات إنشاء كلمات المرور وإدارتها وحماية حسابات الموظفين.",tags:["أمن معلومات","حسابات"],author:"محمد الغامدي",date:"منذ 3 أيام",favorite:true},
  {id:4,title:"منهجية توثيق اجتماعات العملاء",category:"المبيعات",type:"مقال",summary:"أفضل الممارسات لتسجيل احتياجات العميل والقرارات والخطوات التالية.",tags:["عملاء","اجتماعات"],author:"سارة الزهراني",date:"منذ 5 أيام",favorite:false},
  {id:5,title:"خطة استمرارية الأعمال",category:"العمليات",type:"سياسة",summary:"إطار العمل المعتمد لاستمرارية العمليات الحرجة والتعامل مع حالات التعطل.",tags:["مخاطر","استمرارية"],author:"إدارة المخاطر",date:"منذ أسبوع",favorite:false},
  {id:6,title:"دليل استخدام نظام التذاكر",category:"التقنية",type:"دليل",summary:"شرح مبسط لفتح التذاكر ومتابعتها وتصعيدها إلى فرق الدعم المختصة.",tags:["دعم فني","أنظمة"],author:"فريق التقنية",date:"منذ أسبوع",favorite:false}
];

let articles = JSON.parse(localStorage.getItem("manara_articles") || "null") || seedArticles;
const views = document.querySelectorAll(".view");
const navItems = document.querySelectorAll(".nav-item");

function save(){ localStorage.setItem("manara_articles", JSON.stringify(articles)); }

function goTo(view){
  views.forEach(v=>v.classList.remove("active"));
  navItems.forEach(n=>n.classList.remove("active"));
  document.getElementById(view+"View").classList.add("active");
  document.querySelector(`[data-view="${view}"]`)?.classList.add("active");
  if(view==="library") renderLibrary();
  if(view==="favorites") renderFavorites();
}

navItems.forEach(item=>item.addEventListener("click",()=>goTo(item.dataset.view)));
document.querySelectorAll("[data-goto]").forEach(b=>b.addEventListener("click",()=>goTo(b.dataset.goto)));

function articleCard(a){
  return `<article class="knowledge-card">
    <div class="card-top"><span class="card-type">${a.type} · ${a.category}</span><button class="favorite-btn ${a.favorite?'active':''}" onclick="toggleFavorite(${a.id})">${a.favorite?'★':'☆'}</button></div>
    <h3>${escapeHtml(a.title)}</h3>
    <p>${escapeHtml(a.summary)}</p>
    <div class="tag-list">${a.tags.map(t=>`<span class="tag">#${escapeHtml(t)}</span>`).join("")}</div>
    <div class="card-footer"><span>${escapeHtml(a.author)}</span><span>${a.date}</span></div>
  </article>`;
}

function renderRecent(){
  const el = document.getElementById("recentList");
  el.innerHTML = articles.slice(0,4).map(a=>`
    <div class="article-item">
      <div class="doc-icon">▤</div>
      <div class="article-info"><strong>${escapeHtml(a.title)}</strong><div class="meta"><span class="pill">${a.category}</span><span>${a.author}</span><span>${a.date}</span></div></div>
      <button class="favorite-btn ${a.favorite?'active':''}" onclick="toggleFavorite(${a.id})">${a.favorite?'★':'☆'}</button>
    </div>`).join("");
  document.getElementById("totalArticles").textContent = 122 + articles.length;
}

function filterArticles(){
  const query = document.getElementById("globalSearch").value.trim().toLowerCase();
  const category = document.getElementById("categoryFilter")?.value || "الكل";
  return articles.filter(a=>{
    const text = [a.title,a.summary,a.category,a.type,...a.tags].join(" ").toLowerCase();
    return text.includes(query) && (category==="الكل" || a.category===category);
  });
}

function renderLibrary(){
  const data = filterArticles();
  document.getElementById("libraryGrid").innerHTML = data.length ? data.map(articleCard).join("") : `<div class="empty">لا توجد نتائج مطابقة للبحث.</div>`;
}
function renderFavorites(){
  const data = articles.filter(a=>a.favorite);
  document.getElementById("favoritesGrid").innerHTML = data.length ? data.map(articleCard).join("") : `<div class="empty">لم تضف أي معرفة إلى المفضلة بعد.</div>`;
}
window.toggleFavorite = id => {
  articles = articles.map(a=>a.id===id?{...a,favorite:!a.favorite}:a);
  save(); renderRecent(); renderLibrary(); renderFavorites();
};

const modal = document.getElementById("modal");
const openModal = ()=>modal.classList.add("open");
const closeModal = ()=>modal.classList.remove("open");
document.getElementById("openModalBtn").onclick=openModal;
document.getElementById("closeModalBtn").onclick=closeModal;
document.getElementById("cancelBtn").onclick=closeModal;
modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});

document.getElementById("articleForm").addEventListener("submit",e=>{
  e.preventDefault();
  const article = {
    id:Date.now(),
    title:document.getElementById("titleInput").value.trim(),
    category:document.getElementById("categoryInput").value,
    type:document.getElementById("typeInput").value,
    summary:document.getElementById("summaryInput").value.trim(),
    tags:document.getElementById("tagsInput").value.split(",").map(x=>x.trim()).filter(Boolean),
    author:"عبدالله المالكي",
    date:"الآن",
    favorite:false
  };
  articles.unshift(article); save(); e.target.reset(); closeModal(); renderRecent(); renderLibrary();
  const toast=document.getElementById("toast"); toast.classList.add("show"); setTimeout(()=>toast.classList.remove("show"),2200);
});

document.getElementById("globalSearch").addEventListener("input",()=>{
  if(document.getElementById("globalSearch").value.trim()) goTo("library");
  renderLibrary();
});
document.getElementById("categoryFilter").addEventListener("change",renderLibrary);
document.addEventListener("keydown",e=>{
  if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();document.getElementById("globalSearch").focus()}
  if(e.key==="Escape")closeModal();
});

function escapeHtml(v){return String(v).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
renderRecent(); renderLibrary(); renderFavorites();

