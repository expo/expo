import React, { useState, useEffect } from 'react';
import { View, Text, Button, Image, TouchableOpacity, Alert, StyleSheet } from 'react-native';

const SERVER_URL = 'http://your-server-url.com'; // replace with real IP/server

export default function App() {
  const [coins, setCoins] = useState(100);
  const [spinning, setSpinning] = useState(false);

  const handleSpin = async () => {
    setSpinning(true);
    try {
      const response = await fetch(`${SERVER_URL}/spin`, { method: 'POST' });
      const data = await response.json();
      setCoins(prev => prev + data.reward);
      Alert.alert(`🎉 You won ${data.reward} coins!`);
    } catch (err) {
      Alert.alert('Error spinning wheel.');
    }
    setSpinning(false);
  };

  return (
    <View style={styles.container}>
      <Text style={styles.header}>🎰 Spin Boss BD</Text>
      <Text style={styles.coins}>Your Coins: {coins}</Text>

      <TouchableOpacity onPress={handleSpin} style={styles.spinButton} disabled={spinning}>
        <Text style={styles.spinText}>{spinning ? 'Spinning...' : 'Spin Now'}</Text>
      </TouchableOpacity>

      <TouchableOpacity
        style={styles.telegramButton}
        onPress={() => {
          Alert.alert('Redirecting to Telegram...');
        }}
      >
        <Text style={styles.telegramText}>Join Telegram Group</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#111', alignItems: 'center', justifyContent: 'center' },
  header: { fontSize: 28, color: '#fff', fontWeight: 'bold', marginBottom: 20 },
  coins: { fontSize: 20, color: '#ff0', marginBottom: 20 },
  spinButton: { backgroundColor: '#28a745', padding: 15, borderRadius: 10 },
  spinText: { color: '#fff', fontSize: 18 },
  telegramButton: { marginTop: 30 },
  telegramText: { color: '#00f', fontSize: 16 },
});
const express = require('express');
const cors = require('cors');
const app = express();

app.use(cors());
app.use(express.json());

// Simple coin reward logic with RTP
function getSpinReward() {
  const rtp = 90; // 90% RTP
  const probabilities = [
    { reward: 1000, chance: 0.0000005 }, // Grand prize
    { reward: 10, chance: 0.2 },
    { reward: 5, chance: 0.3 },
    { reward: 1, chance: 0.5 },
    { reward: 0, chance: 0.1 },
  ];

  let rand = Math.random();
  let sum = 0;
  for (let item of probabilities) {
    sum += item.chance;
    if (rand < sum) return item.reward;
  }
  return 1;
}

app.post('/spin', (req, res) => {
  const reward = getSpinReward();
  res.json({ reward });
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`✅ Server running on port ${PORT}`));
cd frontend
npm install
npx expo start
