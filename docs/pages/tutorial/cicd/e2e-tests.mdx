---
title: 'EAS Workflows for E2E tests'
sidebar_title: E2E tests
description: Learn how to automate end-to-end (E2E) tests with EAS Workflows.
---

import { ContentSpotlight } from '~/ui/components/ContentSpotlight';
import { ProgressTracker } from '~/ui/components/ProgressTracker';
import { Terminal } from '~/ui/components/Snippet';
import { Step } from '~/ui/components/Step';

In this chapter, we'll create a workflow that runs E2E tests using Maestro on Android Emulator and iOS Simulator against development builds. Maestro is the most popular E2E testing tool among non-enterprise Expo users, and the [`maestro`](/eas/workflows/pre-packaged-jobs/#maestro) pre-packaged job makes it straightforward to integrate into EAS Workflows.

> **important** The Maestro job type in EAS Workflows is currently in [alpha](/more/release-statuses/#alpha).

## Why are E2E tests useful?

The pipeline from previous chapters builds our project, delivers updates, and notifies teams over Slack. However, a broken navigation flow or a crashing screen can still make it to production silently. E2E tests can help catch these issues before they reach production by simulating real user interactions and verifying that the app behaves as expected.

[Maestro](https://maestro.mobile.dev/) runs automated flows against a built app on an Android Emulator or iOS Simulator. It can tap buttons, type text, assert screen content, and navigate between screens. EAS Workflows runs these flows in the cloud after building an APK for Android or an iOS Simulator build.

## Set up E2E tests

For Android and iOS, we need to add a special build profile for E2E tests. This profile skips creating credential setup for Android and allows running our iOS build inside a simulator.

<Step label="1">

### Add the e2e-test build profile

Add an `e2e-test` profile to **eas.json**. This profile creates an unsigned APK for Android and a simulator build for iOS:

```json eas.json
{
  /* @hide ... */ /* @end */
  "build": {
    "e2e-test": {
      "withoutCredentials": true,
      "android": {
        "buildType": "apk",
        "image": "latest"
      },
      "ios": {
        "simulator": true,
        "image": "latest"
      }
    }
  }
}
```

</Step>

<Step label="2">

### Add Maestro tests flow files

Create a **.maestro/** directory at the root of the project and then add the following test files.

For example, the following code snippet adds a basic home screen test that launches the app and checks that the "Welcome" text is visible:

```yaml .maestro/home.yml
appId: com.yourname.yourapp # Replace with your app's package name/bundle identifier from app config file
---
- launchApp
- assertVisible: 'Welcome'
```

Then, we can add another test for the settings screen:

```yaml .maestro/navigate.yml
appId: com.yourname.yourapp # Replace with your app's package name/bundle identifier from app config file
---
- launchApp
- tapOn: 'Settings'
- assertVisible: 'Settings'
```

</Step>

## Create a workflow for E2E tests

Now that we have our build profile and Maestro flow files set up, we can create a workflow to run our E2E tests.

<Step label="1">

### Create an e2e-tests.yml

Inside **.eas/workflows/**, add a new file called **e2e-tests.yml** with the following code:

```yaml .eas/workflows/e2e-tests.yml
name: E2E Tests

jobs:
  build_android:
    name: Build Android for E2E
    type: build
    params:
      platform: android
      profile: e2e-test
  build_ios:
    name: Build iOS for E2E
    type: build
    params:
      platform: ios
      profile: e2e-test
  test_android:
    name: Run Maestro tests (Android)
    needs: [build_android]
    type: maestro
    params:
      build_id: ${{ needs.build_android.outputs.build_id }}
      flow_path: ['.maestro/home.yml', '.maestro/navigate.yml']
  test_ios:
    name: Run Maestro tests (iOS)
    needs: [build_ios]
    type: maestro
    params:
      build_id: ${{ needs.build_ios.outputs.build_id }}
      flow_path: ['.maestro/home.yml', '.maestro/navigate.yml']
```

In the above workflow, there are two build jobs for Android and iOS: `build_android` and `build_ios`. After each build job, there is a corresponding Maestro job: `test_android` and `test_ios`, which run the Maestro tests for each platform.

Each Maestro job takes the `build_id` output from its respective build job to know which build to run the tests on, and it also uses `flow_path` to specify the path to the Maestro flow files we created earlier.

</Step>

<Step label="2">

### Run the workflow manually

Use `eas workflow:run` command to run the workflow manually:

<Terminal cmd={['$ eas workflow:run .eas/workflows/e2e-tests.yml']} />

On the EAS dashboard, notice that the Android and iOS builds start in parallel. Once each build completes, the corresponding Maestro test job runs the flow files against it.

<ContentSpotlight
  alt="EAS Workflows run with Maestro jobs."
  src="/static/images/tutorial/cicd/e2e-01.png"
  className="max-w-160"
/>

<ContentSpotlight
  alt="Successful Maestro jobs run displayed in EAS dashboard."
  src="/static/images/tutorial/cicd/e2e-02.png"
  className="max-w-100"
/>

</Step>

## Trigger E2E tests automatically

So far we've run the workflow manually. For a team pipeline, we can trigger it from GitHub events using one of the following methods:

### Run on every pull request

To run E2E tests on every pull request, add an `on.pull_request` trigger to the workflow:

```yaml .eas/workflows/e2e-tests.yml
on:
  pull_request:
    branches: ['*']
```

### Run on demand with a label

To run E2E tests only when needed, use the [`on.pull_request_labeled`](/eas/workflows/syntax/#onpull_request_labeled) trigger. It accepts a `labels` parameter, and the workflow will only run when one of those labels is added to a pull request. For example, we can use the `test` label to trigger our E2E tests workflow:

```yaml .eas/workflows/e2e-tests.yml
on:
  pull_request_labeled:
    labels: ['test']
```

With the `pull_request_labeled` trigger, the workflow will only run when a "test" label is added to a PR in GitHub.

<ContentSpotlight
  alt="EAS Workflows run with Maestro jobs using a pull request label."
  src="/static/images/tutorial/cicd/e2e-03.png"
  className="max-w-160"
/>

## Summary

<ProgressTracker
  currentChapterIndex={3}
  name="CICD_TUTORIAL"
  summary="We created a Maestro E2E test workflow on dev builds for Android and iOS, kept the trigger manual by default, and learned how to use a pull request label trigger for on-demand runs."
  nextChapterDescription="In the next chapter, learn how to create a production workflow with fingerprinting and OTA updates."
  nextChapterTitle="Production deployments"
  nextChapterLink="/tutorial/cicd/production-deployments/"
/>
