VioletBay/
 ├─ App.js
 ├─ components/
 │   └─ ProductCard.js
 └─ data/
     └─ products.jsimport React from 'react';
import { View, Text, StyleSheet, FlatList, SafeAreaView } from 'react-native';
import ProductCard from './components/ProductCard';
import products from './data/products';

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.header}>VioletBay</Text>

      <FlatList
        data={products}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => <ProductCard product={item} />}
      />
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
  },
  header: {
    backgroundColor: '#6A0DAD',
    color: '#FFFFFF',
    fontSize: 26,
    fontWeight: 'bold',
    textAlign: 'center',
    padding: 15,
  },
});import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

export default function ProductCard({ product }) {
  return (
    <TouchableOpacity style={styles.card}>
      <Text style={styles.title}>{product.title}</Text>
      <Text style={styles.price}>€ {product.price}</Text>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#F4ECF7',
    margin: 10,
    padding: 15,
    borderRadius: 10,
    borderLeftWidth: 5,
    borderLeftColor: '#6A0DAD',
  },
  title: {
    fontSize: 18,
    fontWeight: '600',
    color: '#4A235A',
  },
  price: {
    fontSize: 16,
    marginTop: 5,
    color: '#6A0DAD',
    fontWeight: 'bold',
  },
});export default [
  { id: '1', title: 'iPhone 13 Usato', price: 650 },
  { id: '2', title: 'PlayStation 5', price: 480 },
  { id: '3', title: 'Scarpe Nike Air', price: 120 },
  { id: '4', title: 'Laptop Gaming', price: 900 },
];npm install -g expo-cli
expo init VioletBay
cd VioletBay
expo start
