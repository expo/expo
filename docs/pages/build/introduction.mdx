# Install Expo CLI if you haven’t already
npm install -g expo-cli

# Create a new blank project
expo init QuestForTheCrown
cd QuestForTheCrown

# Add some handy libraries
expo install react-native-svg @react-native-community/hooks

export type TileType = 'empty' | 'crown' | 'trap' | 'item';

export interface Tile {
  id: string;          // unique key
  row: number;
  col: number;
  type: TileType;
}

export interface Player {
  id: string;
  name: string;
  color: string;
  row: number;
  col: number;
  gold: number;
  items: string[];    // simple names, could be expanded
}


import { Tile } from '../types';
import uuid from 'react-native-uuid';

export function generateBoard(size = 5): Tile[] {
  const tiles: Tile[] = [];

  // create grid
  for (let r = 0; r < size; r++) {
    for (let c = 0; c < size; c++) {
      tiles.push({
        id: uuid.v4() as string,
        row: r,
        col: c,
        type: 'empty',
      });
    }
  }

  // pick a random tile to be the crown
  const crownIdx = Math.floor(Math.random() * tiles.length);
  tiles[crownIdx].type = 'crown';

  // add some traps and items (optional)
  for (let i = 0; i < size; i++) {
    const trapIdx = Math.floor(Math.random() * tiles.length);
    if (tiles[trapIdx].type === 'empty') tiles[trapIdx].type = 'trap';
  }

  return tiles;
}


import React from 'react';
import { TouchableOpacity, Text, StyleSheet } from 'react-native';

export const Dice: React.FC<{ onRoll: (value: number) => void }> = ({ onRoll }) => {
  const roll = () => Math.floor(Math.random() * 6) + 1;

  return (
    <TouchableOpacity style={styles.button} onPress={() => onRoll(roll())}>
      <Text style={styles.text}>ROLL DICE</Text>
    </TouchableOpacity>
  );
};

const styles = StyleSheet.create({
  button: { padding: 12, backgroundColor: '#4a90e2', borderRadius: 8 },
  text: { color: 'white', fontSize: 18, fontWeight: 'bold' },
});


import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Tile as TileType } from '../types';

export const Tile: React.FC<{ tile: TileType; player?: boolean }> = ({ tile, player }) => {
  let bgColor = '#eee';
  if (tile.type === 'crown') bgColor = '#ffd700';
  else if (tile.type === 'trap') bgColor = '#ff6666';
  else if (tile.type === 'item') bgColor = '#66cc66';

  return (
    <View style={[styles.tile, { backgroundColor: bgColor }]}>
      {player && <Text style={styles.player}>✦</Text>}
      {tile.type === 'crown' && <Text style={styles.crown}>👑</Text>}
    </View>
  );
};

const styles = StyleSheet.create({
  tile: {
    width: 60,
    height: 60,
    borderWidth: 1,
    borderColor: '#999',
    alignItems: 'center',
    justifyContent: 'center',
  },
  player: { fontSize: 20, color: '#000' },
  crown: { fontSize: 18 },
});



import React, { useState } from 'react';
import { SafeAreaView, View, Text, StyleSheet, FlatList } from 'react-native';
import { generateBoard } from './src/utils/board';
import { Tile as TileType } from './src/types';
import { Dice } from './src/components/Dice';
import { Tile } from './src/components/Tile';

const BOARD_SIZE = 5;

export default function App() {
  const [tiles, setTiles] = useState<TileType[]>(generateBoard(BOARD_SIZE));

  // single player for demo; extend to multiple players as needed
  const [playerPos, setPlayerPos] = useState({ row: 0, col: 0 });
  const [gold, setGold] = useState(10);
  const [diceValue, setDiceValue] = useState<number | null>(null);

  // find the tile at a given coordinate
  const getTileAt = (row: number, col: number) =>
    tiles.find((t) => t.row === row && t.col === col);

  const rollDice = () => {
    const value = Math.floor(Math.random() * 6) + 1;
    setDiceValue(value);
    movePlayer(value);
  };

  const movePlayer = (steps: number) => {
    // simple linear movement: move right, then down when at edge
    let { row, col } = playerPos;
    for (let i = 0; i < steps; i++) {
      if (col + 1 < BOARD_SIZE) col += 1;
      else if (row + 1 < BOARD_SIZE) { col = 0; row += 1; }
      // otherwise stay in place
    }
    setPlayerPos({ row, col });

    const landedTile = getTileAt(row, col);
    if (!landedTile) return;

    if (landedTile.type === 'trap') {
      alert('You triggered a trap! Lose 2 Gold.');
      setGold((g) => Math.max(g - 2, 0));
    } else if (landedTile.type === 'crown') {
      alert(`🎉 You found the crown and win with ${gold} gold left!`);
    }
  };

  const renderItem = ({ item }: { item: TileType }) => (
    <Tile
      tile={item}
      player={
        item.row === playerPos.row && item.col === playerPos.col
      }
    />
  );

  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.title}>Quest for the Crown</Text>
      <FlatList
        data={tiles}
        renderItem={renderItem}
        keyExtractor={(t) => t.id}
        numColumns={BOARD_SIZE}
        scrollEnabled={false}
      />
      <View style={styles.info}>
        <Text>Gold: {gold}</Text>
        {diceValue !== null && <Text>Last Roll: {diceValue}</Text>}
      </View>
      <Dice onRoll={rollDice} />
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', paddingTop: 20 },
  title: { fontSize: 24, marginBottom: 10 },
  info: { marginVertical: 10 },
});


---
title: EAS Build
sidebar_title: Introduction
hideTOC: true
description: EAS Build is a hosted service for building app binaries for your Expo and React Native projects.
---

import { Cube01Icon } from '@expo/styleguide-icons/outline/Cube01Icon';

import { BoxLink } from '~/ui/components/BoxLink';

**EAS Build** is a hosted service for building app binaries for your Expo and React Native projects.

It makes building your apps for distribution simple and easy to automate by providing defaults that work well for Expo and React Native projects out of the box, and by handling your app signing credentials for you (if you wish). It also makes sharing builds with your team easier than ever with [internal distribution](/build/internal-distribution/) (using ad hoc and/or enterprise "universal" provisioning), deeply integrates with EAS Submit for app store submissions, and has first-class support for the [`expo-updates`](/build/updates/) library.

It's designed to work for any native project, whether or not you use Expo and React Native. It's the fastest way to get from `npx create-expo-app` or `npx @react-native-community/cli@latest init` to app stores.

### Get started

<BoxLink
  title="Create your first build"
  description="It should only take a few minutes in total to get up and running for iOS and/or Android."
  href="/build/setup"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Share your apps with internal testers"
  description="EAS Build can help share preview builds of your app with a single URL."
  href="/build/internal-distribution"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Automate submissions"
  description="Learn how you can have the service take your successful builds and handle uploading them to app stores for you automatically."
  href="/build/automate-submissions"
  Icon={Cube01Icon}
/>

<BoxLink
  title="App version management"
  description="Automate version bumps so you never have to think about them again."
  href="/build-reference/app-versions"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Run builds locally or on your own infrastructure"
  description="EAS Build is a hosted service, but you can also run it on your own machine, for example, to debug or to comply with any company security policies."
  href="/build-reference/local-builds"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Limitations"
  description="EAS Build is new and rapidly evolving, so we recommend getting familiar with the current limitations."
  href="/build-reference/limitations"
  Icon={Cube01Icon}
/>
