import { LinearGradient } from 'expo-linear-gradient';
import { View, Text } from 'react-native';

export default function App() {
  return (
    <LinearGradient
      colors={['#ff9cf7', '#ffd6f6', '#b7e8ff']}
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      }}
    >
      <Text
        style={{
          fontSize: 40,
          color: 'white',
          fontWeight: 'bold'
        }}
      >
        ✨ FLAZZ
      </Text>
    </LinearGradient>
  );
}
