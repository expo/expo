import React from 'react';
import { Text, SafeAreaView, StyleSheet, View, Linking, TouchableOpacity } from 'react-native';
import { Card } from 'react-native-paper';

export default function App() {
  const openLink = (url) => {
    Linking.openURL(url).catch((err) => console.error('Error opening URL:', err));
  };

  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.title}>Heavy Granite House</Text>

      <Card style={styles.card}>
        <Text style={styles.paragraph}>
          Explore premium quality granite, quartz, marble, and stone for all your home improvement needs.
        </Text>
        <TouchableOpacity onPress={() => openLink('https://heavygranitehouse.com/')}>
          <Text style={styles.link}>Visit Heavy Granite House</Text>
        </TouchableOpacity>
      </Card>

      <Card style={styles.card}>
        <Text style={styles.paragraph}>
          Check out our stunning Fantasy Brown Granite slabs, perfect for your modern interiors.
        </Text>
        <TouchableOpacity onPress={() => openLink('https://heavygranitehouse.com/product/granite-slab/fantasy-brown/')}>
          <Text style={styles.link}>View Fantasy Brown Granite</Text>
        </TouchableOpacity>
      </Card>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f8f9fa',
    padding: 16,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 16,
    color: '#2c3e50',
  },
  card: {
    width: '90%',
    marginBottom: 16,
    padding: 16,
    backgroundColor: '#ffffff',
    borderRadius: 8,
    elevation: 4,
  },
  paragraph: {
    fontSize: 16,
    color: '#34495e',
    marginBottom: 8,
  },
  link: {
    fontSize: 16,
    color: '#2980b9',
    textDecorationLine: 'underline',
  },
});
