---
title: Creating a native view module
---

import { CodeBlocksTable } from '~/components/plugins/CodeBlocksTable';
import { PlatformTag } from '~/ui/components/Tag';
import { APIBox } from '~/components/plugins/APIBox';


In this tutorial we are going to build a view on the native side, render it from React Native as a component, and customize it with props.

- what do we want to build? open to ideas