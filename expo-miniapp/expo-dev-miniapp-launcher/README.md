# expo-dev-miniapp-launcher

Expo 小程序容器的自定义开发启动器 - **完全独立于** `expo-dev-launcher`。

## 为什么要单独创建一个包？

这个包是开发启动器 UI 的完整重新实现，专为小程序容器项目设计。通过创建独立的包而不是修改 `expo-dev-launcher`，我们获得了：

- ✅ 更新上游 Expo 仓库时避免冲突
- ✅ 完全控制 UI 和功能
- ✅ 独立迭代，不影响原始包
- ✅ 保持代码库整洁可维护

## 功能特性

- 🎨 **自定义 UI** - 专为小程序加载设计的现代化界面
- 📱 **二维码扫描** - 快速连接到开发服务器
- 🔌 **手动输入 URL** - 连接到任何开发服务器
- 📚 **最近使用** - 追踪最近打开的小程序
- 🌐 **完全兼容** - 与标准 Expo 开发工作流兼容

## 安装

```bash
npm install expo-dev-miniapp-launcher
```

或使用 `create-expo-miniapp-container` CLI：

```bash
npx create-expo-miniapp-container my-container
```

## 配置

在 `app.json` 中添加：

```json
{
  "expo": {
    "plugins": [
      [
        "expo-dev-miniapp-launcher",
        {
          "enabled": true,
          "launchMode": "launcher"
        }
      ]
    ]
  }
}
```

### 配置选项

- `enabled` (boolean, 默认: `true`) - 启用或禁用启动器
- `launchMode` (string, 默认: `'launcher'`) - 启动模式：
  - `'launcher'` - 总是显示启动器 UI
  - `'most-recent'` - 尝试先加载最近的应用

## 使用方法

运行以下命令时，启动器会自动显示：

```bash
npx expo run:android
# 或
npx expo run:ios
```

### 主要功能

1. **扫描二维码** - 将相机对准 `npx expo start` 显示的二维码
2. **手动输入 URL** - 输入开发服务器地址（例如：`http://192.168.1.100:8081`）
3. **最近使用** - 快速访问之前加载的应用

## 开发指南

### 构建

```bash
cd expo-miniapp/expo-dev-miniapp-launcher
npm run build
```

### 测试

```bash
# 创建测试项目
npx create-expo-miniapp-container test-container
cd test-container

# 链接到本地包
npm install file:../expo-miniapp/expo-dev-miniapp-launcher

# 运行
npx expo run:android
```

## 架构

```
expo-dev-miniapp-launcher/
├── android/                    # Android 原生代码
│   └── src/
│       ├── main/              # 主模块
│       │   ├── DevMiniAppLauncherModule.kt
│       │   └── DevMiniAppLauncherPackage.kt
│       └── debug/             # Debug 专用 UI
│           └── ui/
│               └── MiniAppHomeScreen.kt  # 自定义 Compose UI
├── ios/                       # iOS 原生代码 (待实现)
├── plugin/                    # Expo 配置插件
│   └── src/
│       └── index.ts
└── package.json
```

## 与 expo-dev-launcher 的对比

| 特性 | expo-dev-launcher | expo-dev-miniapp-launcher |
|------|-------------------|---------------------------|
| 用途 | 通用 Expo 开发 | 专为小程序容器设计 |
| UI | 标准开发工具 | 自定义小程序 UI |
| 更新 | 由 Expo 团队管理 | 独立版本控制 |
| 自定义 | 有限 | 完全控制 |
| 冲突 | 更新时可能冲突 | 无冲突 |

## 路线图

- [x] Android 自定义 UI
- [ ] iOS 自定义 UI (SwiftUI)
- [ ] 小程序 manifest 解析器
- [ ] 离线包加载器
- [ ] 小程序权限系统
- [ ] 小程序热重载

## UI 预览

启动器包含以下界面元素：

### 欢迎横幅
- 🚀 紫色渐变背景
- 清晰的品牌标识
- 功能说明文字

### 二维码扫描
- 📷 大按钮，易于点击
- 相机权限请求
- 扫描反馈

### 手动输入
- ✍️ URL 输入框
- 连接按钮
- 验证提示

### 最近使用
- 📚 应用列表
- 快速重连
- 删除选项

## 技术栈

### Android
- **Kotlin** - 编程语言
- **Jetpack Compose** - UI 框架
- **Material Design 3** - 设计规范
- **Expo Modules API** - 模块集成

### iOS (计划中)
- **Swift** - 编程语言
- **SwiftUI** - UI 框架
- **Expo Modules API** - 模块集成

## 贡献

欢迎贡献！这个包是小程序容器项目的一部分。

## 相关文档

- [create-expo-miniapp-container](../create-expo-miniapp-container/README.md)
- [expo-miniapp 总览](../README.md)
- [测试指南](../create-expo-miniapp-container/TESTING.md)

## 许可证

MIT
