import { Button, Text, View } from 'react-native';

export default function RouteInSlot() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#000',
      }}>
      <Text style={{ color: '#fff' }}>Route in Slot</Text>
    </View>
  );
}
