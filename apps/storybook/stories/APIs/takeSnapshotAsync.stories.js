import React from 'react';
import { View } from 'react-native';

export const title = 'takeSnapshotAsync';
export const packageJson = require('expo/package.json');
export const component = () => <View />;
