import React from 'react';
import { View } from 'react-native';

export const title = 'Payments';
export const packageJson = require('expo-payments-stripe/package.json');
export const component = () => <View />;
