import React from 'react';
import { View } from 'react-native';

export const title = 'Keep Awake';
export const packageJson = require('expo-keep-awake/package.json');
export const component = () => <View />;
