// Put `expo` package placeholders here so webpack doesn't print a lot of warnings that something can't be imported.
// They can be removed once the module gets extracted into separate package.

export const AuthSession = {};

export const DangerZone = {};

export const Linking = {};

export const Notifications = {
  addListener: () => null,
};

export const ScreenOrientation = {};
