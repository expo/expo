diff --git a/RNReanimated.podspec b/RNReanimated.podspec
--- a/RNReanimated.podspec
+++ b/RNReanimated.podspec
@@ -67,6 +67,7 @@ Pod::Spec.new do |s|
       '"$(PODS_ROOT)/Headers/Private/React-Core"',
       '"$(PODS_ROOT)/Headers/Private/Yoga"',
       '"$(PODS_ROOT)/Headers/Public/RNWorklets"',
+      '"${PODS_CONFIGURATION_BUILD_DIR}/React-jsitooling/JSITooling.framework/Headers"',
     ].join(' '),
     "FRAMEWORK_SEARCH_PATHS" => '"${PODS_CONFIGURATION_BUILD_DIR}/React-hermes"',
     "CLANG_CXX_LANGUAGE_STANDARD" => "c++20",
@@ -94,5 +95,6 @@ Pod::Spec.new do |s|
   install_modules_dependencies(s)
 
   s.dependency 'React-jsi'
+  s.dependency 'React-jsitooling'
   s.dependency 'React-hermes'
 end
diff --git a/Common/cpp/reanimated/NativeModules/ReanimatedModuleProxy.cpp b/Common/cpp/reanimated/NativeModules/ReanimatedModuleProxy.cpp
--- a/Common/cpp/reanimated/NativeModules/ReanimatedModuleProxy.cpp
+++ b/Common/cpp/reanimated/NativeModules/ReanimatedModuleProxy.cpp
@@ -881,6 +881,7 @@
 }
 
 std::shared_ptr<UIManagerAnimationBackend> ReanimatedModuleProxy::getAnimationBackend() {
+#if REACT_NATIVE_VERSION_MINOR >= 85
   react_native_assert(
       uiManager_ != nullptr && "[Reanimated] Animation Backend used before the uiManager was registered");
   auto locked = uiManager_->unstable_getAnimationBackend().lock();
@@ -889,6 +890,9 @@
       locked != nullptr &&
       "[Reanimated] Animation Backend is null (UIManager not wired to a backend yet or already torn down)");
   return locked;
+#else
+  return nullptr;
+#endif
 }
 
 void ReanimatedModuleProxy::startBackendIfNeeded() {
@@ -1176,6 +1180,7 @@
   uiManager_ = uiManager;
   viewStylesRepository_->setUIManager(uiManager_);
 
+#if REACT_NATIVE_VERSION_MINOR >= 85
   if constexpr (StaticFeatureFlags::getFlag("USE_ANIMATION_BACKEND")) {
     if (!ReactNativeFeatureFlags::useSharedAnimatedBackend()) {
       react_native_assert(
@@ -1186,6 +1191,7 @@
           "or disable the Reanimated Feature Flag");
     }
   }
+#endif
 
   initializeLayoutAnimationsProxyRegistry();
 
diff --git a/Common/cpp/reanimated/NativeModules/PropValueProcessor.cpp b/Common/cpp/reanimated/NativeModules/PropValueProcessor.cpp
--- a/Common/cpp/reanimated/NativeModules/PropValueProcessor.cpp
+++ b/Common/cpp/reanimated/NativeModules/PropValueProcessor.cpp
@@ -1,4 +1,5 @@
 #include <jsi/jsi.h>
+#include <reanimated/Compat/ReactNativeVersionCompat.h>
 #include <reanimated/NativeModules/PropValueProcessor.h>
 
 #include <iomanip>
@@ -117,12 +118,17 @@
 
 jsi::Object PropValueProcessor::linearGradientPreprocessing(const LinearGradient &linearGradient, jsi::Runtime &rt) {
   jsi::Object direction(rt);
-  if (std::holds_alternative<Float>(linearGradient.direction)) {
+#if REACT_NATIVE_VERSION_MINOR >= 86
+  const auto &gradientDirection = linearGradient.direction;
+#else
+  const auto &gradientDirection = linearGradient.direction.value;
+#endif
+  if (std::holds_alternative<Float>(gradientDirection)) {
     direction.setProperty(rt, "type", "angle");
-    direction.setProperty(rt, "value", static_cast<double>(std::get<Float>(linearGradient.direction)));
+    direction.setProperty(rt, "value", static_cast<double>(std::get<Float>(gradientDirection)));
   } else {
     direction.setProperty(rt, "type", "keyword");
-    switch (std::get<GradientKeyword>(linearGradient.direction)) {
+    switch (std::get<GradientKeyword>(gradientDirection)) {
       case GradientKeyword::ToTopRight:
         direction.setProperty(rt, "value", "to top right");
         break;
