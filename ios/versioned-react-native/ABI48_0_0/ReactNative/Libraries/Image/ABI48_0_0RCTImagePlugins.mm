/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @generated by an internal plugin build system
 */

#ifndef ABI48_0_0RN_DISABLE_OSS_PLUGIN_HEADER

// OSS-compatibility layer

#import "ABI48_0_0RCTImagePlugins.h"

#import <string>
#import <unordered_map>

Class ABI48_0_0RCTImageClassProvider(const char *name) {
  // Intentionally leak to avoid crashing after static destructors are run.
  static const auto sCoreModuleClassMap = new const std::unordered_map<std::string, Class (*)(void)>{
    {"GIFImageDecoder", ABI48_0_0RCTGIFImageDecoderCls},
    {"ImageEditingManager", ABI48_0_0RCTImageEditingManagerCls},
    {"ImageLoader", ABI48_0_0RCTImageLoaderCls},
    {"ImageStoreManager", ABI48_0_0RCTImageStoreManagerCls},
    {"LocalAssetImageLoader", ABI48_0_0RCTLocalAssetImageLoaderCls},
  };

  auto p = sCoreModuleClassMap->find(name);
  if (p != sCoreModuleClassMap->end()) {
    auto classFunc = p->second;
    return classFunc();
  }
  return nil;
}

#endif // ABI48_0_0RN_DISABLE_OSS_PLUGIN_HEADER
