/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @generated by an internal plugin build system
 */

#ifndef ABI49_0_0RN_DISABLE_OSS_PLUGIN_HEADER

// OSS-compatibility layer

#import "ABI49_0_0RCTAnimationPlugins.h"

#import <string>
#import <unordered_map>

Class ABI49_0_0RCTAnimationClassProvider(const char *name) {
  // Intentionally leak to avoid crashing after static destructors are run.
  static const auto sCoreModuleClassMap = new const std::unordered_map<std::string, Class (*)(void)>{
    {"NativeAnimatedModule", ABI49_0_0RCTNativeAnimatedModuleCls},
    {"NativeAnimatedTurboModule", ABI49_0_0RCTNativeAnimatedTurboModuleCls},
  };

  auto p = sCoreModuleClassMap->find(name);
  if (p != sCoreModuleClassMap->end()) {
    auto classFunc = p->second;
    return classFunc();
  }
  return nil;
}

#endif // ABI49_0_0RN_DISABLE_OSS_PLUGIN_HEADER
