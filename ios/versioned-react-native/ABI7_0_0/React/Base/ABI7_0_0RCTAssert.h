/**
 * Copyright (c) 2015-present, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 */

#import <Foundation/Foundation.h>

#import "ABI7_0_0RCTDefines.h"

/**
 * This is the main assert macro that you should use. Asserts should be compiled out
 * in production builds. You can customize the assert behaviour by setting a custom
 * assert handler through `ABI7_0_0RCTSetAssertFunction`.
 */
#ifndef NS_BLOCK_ASSERTIONS
#define ABI7_0_0RCTAssert(condition, ...) do { \
  if ((condition) == 0) { \
    _ABI7_0_0RCTAssertFormat(#condition, __FILE__, __LINE__, __func__, __VA_ARGS__); \
    if (ABI7_0_0RCT_NSASSERT) { \
      [[NSAssertionHandler currentHandler] handleFailureInFunction:@(__func__) \
        file:@(__FILE__) lineNumber:__LINE__ description:__VA_ARGS__]; \
    } \
  } \
} while (false)
#else
#define ABI7_0_0RCTAssert(condition, ...) do {} while (false)
#endif
ABI7_0_0RCT_EXTERN void _ABI7_0_0RCTAssertFormat(
  const char *, const char *, int, const char *, NSString *, ...
) NS_FORMAT_FUNCTION(5,6);

/**
 * Report a fatal condition when executing. These calls will _NOT_ be compiled out
 * in production, and crash the app by default. You can customize the fatal behaviour
 * by setting a custom fatal handler through `ABI7_0_0RCTSetFatalHandler`.
 */
ABI7_0_0RCT_EXTERN void ABI7_0_0RCTFatal(NSError *error);

/**
 * The default error domain to be used for ReactABI7_0_0 errors.
 */
ABI7_0_0RCT_EXTERN NSString *const ABI7_0_0RCTErrorDomain;

/**
 * JS Stack trace provided as part of an NSError's userInfo
 */
ABI7_0_0RCT_EXTERN NSString *const ABI7_0_0RCTJSStackTraceKey;

/**
 * Name of fatal exceptions generated by ABI7_0_0RCTFatal
 */
ABI7_0_0RCT_EXTERN NSString *const ABI7_0_0RCTFatalExceptionName;

/**
 * A block signature to be used for custom assertion handling.
 */
typedef void (^ABI7_0_0RCTAssertFunction)(
                                  NSString *condition,
                                  NSString *fileName,
                                  NSNumber *lineNumber,
                                  NSString *function,
                                  NSString *message
                                  );

typedef void (^ABI7_0_0RCTFatalHandler)(NSError *error);

/**
 * Convenience macro for asserting that a parameter is non-nil/non-zero.
 */
#define ABI7_0_0RCTAssertParam(name) ABI7_0_0RCTAssert(name, @"'%s' is a required parameter", #name)

/**
 * Convenience macro for asserting that we're running on main thread.
 */
#define ABI7_0_0RCTAssertMainThread() ABI7_0_0RCTAssert([NSThread isMainThread], \
  @"This function must be called on the main thread")

/**
 * Convenience macro for asserting that we're running off the main thread.
 */
#define ABI7_0_0RCTAssertNotMainThread() ABI7_0_0RCTAssert(![NSThread isMainThread], \
@"This function must not be called on the main thread")

/**
 * These methods get and set the current assert function called by the ABI7_0_0RCTAssert
 * macros. You can use these to replace the standard behavior with custom assert
 * functionality.
 */
ABI7_0_0RCT_EXTERN void ABI7_0_0RCTSetAssertFunction(ABI7_0_0RCTAssertFunction assertFunction);
ABI7_0_0RCT_EXTERN ABI7_0_0RCTAssertFunction ABI7_0_0RCTGetAssertFunction(void);

/**
 * This appends additional code to the existing assert function, without
 * replacing the existing functionality. Useful if you just want to forward
 * assert info to an extra service without changing the default behavior.
 */
ABI7_0_0RCT_EXTERN void ABI7_0_0RCTAddAssertFunction(ABI7_0_0RCTAssertFunction assertFunction);

/**
 * This method temporarily overrides the assert function while performing the
 * specified block. This is useful for testing purposes (to detect if a given
 * function asserts something) or to suppress or override assertions temporarily.
 */
ABI7_0_0RCT_EXTERN void ABI7_0_0RCTPerformBlockWithAssertFunction(void (^block)(void), ABI7_0_0RCTAssertFunction assertFunction);

/**
 These methods get and set the current fatal handler called by the ABI7_0_0RCTFatal method.
 */
ABI7_0_0RCT_EXTERN void ABI7_0_0RCTSetFatalHandler(ABI7_0_0RCTFatalHandler fatalHandler);
ABI7_0_0RCT_EXTERN ABI7_0_0RCTFatalHandler ABI7_0_0RCTGetFatalHandler(void);

/**
 * Get the current thread's name (or the current queue, if in debug mode)
 */
ABI7_0_0RCT_EXTERN NSString *ABI7_0_0RCTCurrentThreadName(void);

/**
 * Helper to get generate exception message from NSError
 */
ABI7_0_0RCT_EXTERN NSString *ABI7_0_0RCTFormatError(NSString *message, NSArray<NSDictionary<NSString *, id> *> *stacktrace, NSUInteger maxMessageLength);

/**
 * Convenience macro to assert which thread is currently running (DEBUG mode only)
 */
#if DEBUG

#define ABI7_0_0RCTAssertThread(thread, format...) \
_Pragma("clang diagnostic push") \
_Pragma("clang diagnostic ignored \"-Wdeprecated-declarations\"") \
ABI7_0_0RCTAssert( \
  [(id)thread isKindOfClass:[NSString class]] ? \
    [ABI7_0_0RCTCurrentThreadName() isEqualToString:(NSString *)thread] : \
    [(id)thread isKindOfClass:[NSThread class]] ? \
      [NSThread currentThread] ==  (NSThread *)thread : \
      dispatch_get_current_queue() == (dispatch_queue_t)thread, \
  format); \
_Pragma("clang diagnostic pop")

#else

#define ABI7_0_0RCTAssertThread(thread, format...) do { } while (0)

#endif
