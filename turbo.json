{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "outputs": ["build/**", "dist/**", "lib/**"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "cache": false
    },
    "test": {
      "cache": false
    }
  }
}
