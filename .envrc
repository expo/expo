source_up

# A simple heuristic to determine if we're inside of Universe
if [[ -z "$CI" ]] && [[ ! -z "$EXPO_IN_UNIVERSE" ]]; then
  printf "\033[0;31m⬇️   READ THIS! ⬇️\033[0m\n"
  printf "The exponent directory within Universe is now effectively read-only. The repo at https://github.com/expo/expo is the source of truth. Clone that repo and submit PRs to it from now onward.\n"
fi
