# Vercel AI Gateway (server). Required for live LLM streaming via /api/chat
AI_GATEWAY_API_KEY=

# Optional public flag — when unset the app uses the local demo stream
EXPO_PUBLIC_AI_GATEWAY_API_KEY=

# Production API origin for native builds
EXPO_PUBLIC_API_BASE_URL=
