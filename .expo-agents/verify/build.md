# Building this repository in the sandbox

<!-- The repo's build-and-test recipe. Executed ONLY inside sandboxes; the
runner/worker never runs target-repo code. Measure real timings and record
them the way expo/expo's recipe does. -->
